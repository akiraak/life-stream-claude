# Copy to `.env` and edit for local development.
# When unset, the app falls back to https://basket.chobi.me (see src/config/api-endpoint.ts).
# Use your dev machine's LAN IP so the device can reach it over Wi-Fi.
EXPO_PUBLIC_API_URL=http://192.168.x.x:3000
