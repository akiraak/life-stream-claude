# DONE

完了したタスクの記録。

<!-- 完了日 - タスク名 の形式で記録 -->

## 2026-02
- 2026-02-28 README.md 作成
- 2026-02-28 CLAUDE.md 作成
- 2026-02-28 TODO.md / DONE.md 作成
- 2026-02-28 サーバプロジェクト初期化 (package.json, tsconfig.json, .env)
- 2026-02-28 Express サーバ基盤 (エントリポイント, ヘルスチェック, CORS, エラーハンドリング)
- 2026-02-28 SQLite データベースセットアップ (shopping_items テーブル)
- 2026-02-28 買い物リスト API (CRUD: GET/POST/PUT/DELETE /api/shopping)
- 2026-02-28 Web クライアント: 買い物リスト画面 (HTML/CSS/JS)
- 2026-02-28 管理画面: レイアウト・ナビゲーション
- 2026-02-28 管理画面: データ一覧・インライン編集・削除・全件削除
- 2026-02-28 管理画面: サーバ状態モニタリング (ヘルスチェック + 統計)
- 2026-02-28 管理用 API (GET/PUT/DELETE /api/admin/shopping, GET /api/admin/stats)
- 2026-02-28 Claude Code CLI 連携 (POST /api/claude, claude-service.ts)

## 2026-03
- 2026-03-01 レシピ推薦 API (GET /api/recipes/recommend, recipes.ts)
- 2026-03-01 管理画面: おすすめレシピ UI
- 2026-03-02 リストに料理を追加できるようにし、買い物食材を料理に関連付けられるようにする
- 2026-03-02 食材・料理入力時に購入頻度が高いものをサジェスト表示する
- 2026-03-02 料理追加時にAIで具材を調べて表示し、選択することで買い物リストに追加できるようにする
- 2026-03-02 料理の具材検索はバックグラウンドで行い料理追加画面はすぐに閉じる
- 2026-03-02 料理と食材追加アイコンは縦並びから横並びに
- 2026-03-02 料理名からレシピなどの情報をAIで取得して表示する
- 2026-03-02 食材の削除ボタンを削除
- 2026-03-02 料理の情報（レシピ・具材等）を保存し、次回同じ料理を追加したときに参照できるようにする
- 2026-03-02 具材・レシピダイアログを全画面表示に変更
- 2026-03-02 料理ダイアログにAI再取得ボタンを追加
- 2026-03-02 PWA対応（ホーム画面にアイコン追加可能に）
- 2026-03-02 料理・食材の表示順を長押しドラッグで変更可能に
- 2026-03-02 食材を料理間でドラッグ移動可能に
- 2026-03-02 チェックボックスを四角から丸へ変更
- 2026-03-02 レシピの文章中の食材名をタップ可能にし、買い物リストに追加できるようにする
- 2026-03-02 AI検索中にトースト通知を表示（3秒後にフェードアウト）
- 2026-03-02 料理名を編集できるようにする（レシピページでタップ編集、変更後レシピ再取得）
- 2026-03-02 料理に特定の食材を追加してレシピを再検索できるようにする
- 2026-03-02 レシピ再検索時に指定食材以外も含む本格的なレシピが返るようプロンプト改善
- 2026-03-02 複数ユーザー対応（Magic Link 認証、JWT、ユーザーごとのデータ分離）
- 2026-03-02 食材を上下に移動したときに文字が選択状態になるのを防止
- 2026-03-02 PWA インストール用の Web ページを GitHub Pages で作成する
- 2026-03-02 管理画面を再構築（PC用ダッシュボード：ユーザー管理、全データ管理、システム監視）
- 2026-03-03 AI提案レシピを個別に保存できるようにし、レシピブック画面で閲覧・買い物リスト追加・削除可能に
- 2026-03-03 料理レシピにいいねボタンを追加（ハートアイコン、レシピ再取得時も保持）
- 2026-03-03 料理レシピページ新設（いいね数上位表示、recipe_likesテーブルで複数ユーザーいいね対応）
- 2026-03-03 ヘッダーをハンバーガーメニューに変更（メアド非表示、メニュー内にメアド+ログアウト）
- 2026-03-03 料理レシピページをいいね済みのみ表示に変更（解除時カード削除）
- 2026-03-03 料理のソフトデリート対応（削除→非表示、データ保持）
- 2026-03-03 具材モーダルのレシピにいいねアイコンが出ない問題を修正
- 2026-03-03 料理ソフトデリート時にdish_itemsリンクも削除するよう修正
- 2026-03-03 dish_items中間テーブルをshopping_items.dish_idに統合（スキーマ簡素化）
- 2026-03-03 dish_historyテーブル廃止（dishesテーブルで代替、管理画面から料理履歴ページ削除）
- 2026-03-03 管理画面の料理一覧に紐づき食材を折りたたみ表示、状態カラム追加
- 2026-03-03 料理名編集時にテキストが全選択にならないよう修正（カーソルを末尾に配置）
- 2026-03-03 サーバー再起動時にクライアントが自動リロードするバージョンチェック機能追加
- 2026-03-03 サービスのメールアドレスをGmail→Resend(noreply@chobi.me)に変更
- 2026-03-03 いいねレシピ画面を「みんなのレシピ」「自分のレシピ」に分離、ヘッダーアイコン・メニューから遷移可能に
- 2026-03-03 みんなと自分のレシピ画面に検索機能追加（タイトル・概要・手順をハイライト付きで検索）
- 2026-03-03 レシピ画面に無限スクロール追加（20件ずつ表示）
- 2026-03-03 AIプロンプトをレシピごとの食材返却に変更、レシピ固有の食材を保存
- 2026-03-03 レシピカードに「リストに追加」ボタン追加（具材モーダル・レシピページ両方）
- 2026-03-03 チェック済み食材のハイライト状態を正しく更新するよう修正
- 2026-03-03 Google認証（Sign in with Google）対応追加
- 2026-03-03 アプリ名を「お料理バスケット」に決定（管理画面に候補一覧ページ追加）
- 2026-03-03 マネタイズ検討ドキュメント作成（管理画面にページ追加）
- 2026-03-03 管理画面の企画ドキュメントを1ページに集約（アイコン候補・アプリ名・マネタイズ）
- 2026-03-03 アプリ名を「お料理バスケット」に変更（タイトル、manifest、PWAアイコン表示名など全箇所）
- 2026-03-03 ドメインを life.chobi.me → basket.chobi.me に変更（コード・ドキュメント全箇所）
- 2026-03-03 リポジトリ名を life-stream-claude → cooking-basket に変更（コード・ドキュメント全箇所）
- 2026-03-03 アプリ紹介・インストール手順ページを /about に作成（docs/index.html から移行）
- 2026-03-04 /about ページにアプリスクショ追加・特徴カードにスクショ埋め込みレイアウト採用
- 2026-03-04 デモデータ投入スクリプト作成（seed-demo.ts）
- 2026-03-04 Google認証を一時無効化（テスト完了まで）
- 2026-03-04 iPhoneアプリとAndroidアプリ開発の技術検討（管理画面にドキュメント追加）
- 2026-03-04 WSL2 + Claude Code リモート開発環境検討（管理画面にドキュメント追加）
- 2026-03-04 React Native 開発計画ドキュメント作成（管理画面にページ追加）
- 2026-03-04 Gemini 3.1 Flash-Lite対応（デフォルトモデルをgemini-3.1-flash-lite-previewに変更）
- 2026-03-04 食材/料理追加時の候補選択で即送信
- 2026-03-04 チェック済み食材を折りたたみ表示（10件ずつ追加読み込み）
- 2026-03-04 FAB横並び（料理左・食材右）+ WEB版と同じ料理アイコン
- 2026-03-04 レシピ画面: 追加素材入力廃止、追加した素材を別表示＋再検索ボタン
- 2026-03-04 タブタイトル修正（「自分のレシピ」「みんなのレシピ」）
- 2026-03-04 ログアウト機能（ヘッダーメニュー）
- 2026-03-04 料理ページに「レシピを再取得」ボタン
- 2026-03-04 追加素材（買い物リストから）を別セクションで表示 + 「この素材でレシピを再検索」ボタン
- 2026-03-04 料理名の編集可能表示（破線下線 + ✎アイコン）
- 2026-03-04 再検索ボタン統合（追加素材あり→「この素材で再検索」/ なし→「レシピを再検索」）
- 2026-03-04 料理レシピページの料理名を中央寄せ + 下線表示
- 2026-03-04 React Native Phase 6.1: 料理グループ・食材のドラッグ&ドロップ並び替え
- 2026-03-04 React Native Phase 6.2: アニメーション（既存Animated実装で十分、追加不要）
- 2026-03-04 React Native Phase 6.3: Hapticフィードバック（チェック・ドラッグ・追加・削除）
- 2026-03-04 React Native Phase 6.4: アプリアイコン設定、その他ポリッシュ完了
- 2026-03-04 買い物リストの料理・食材をドラッグで並び替え可能に（reanimated v3 + draggable-flatlist）
- 2026-03-05 「アイテム」を「食材」に名称変更（UI表示・ドキュメント・コメント全箇所）
- 2026-03-05 モバイル版: チェック済みの「すべて削除」ボタンを削除
- 2026-03-05 チェック済み食材は一定量ずつ読み込み済みであることを確認（モバイル版10件ずつ、Web版は非表示）
- 2026-03-05 モバイル版: 食材行の右側×削除ボタンを削除
- 2026-03-05 Web版: 音声入力対応（食材/料理追加モーダルにマイクボタン、Web Speech API）
- 2026-03-05 React Native アプリ Phase 1-6 全完了（初期化・認証・買い物リスト・AI具材&レシピ・レシピブック・ポリッシュ）
- 2026-03-05 EAS セットアップ完了 (eas-cli, eas login, eas init, eas.json作成)
- 2026-03-05 app.json 公開向け設定 (permissions, infoPlist, platforms)
- 2026-03-05 アイコン・スクリーンショット準備（iOS/Android両対応、adaptive-icon修正）
- 2026-03-05 プライバシーポリシーページ作成・公開 (basket.chobi.me/privacy)
- 2026-03-05 Apple Developer Program 登録 ($99/年)
- 2026-03-05 iOS App Store 審査提出（TestFlight動作確認済み）
- 2026-03-05 Google Play Console 登録 ($25)
- 2026-03-05 レシピステップのテキストはみ出し修正（RecipeCard.tsx）
- 2026-03-05 Android adaptive-icon 白背景で再生成
- 2026-03-06 Google Play クローズドテスト トラック作成・AABアップロード・審査提出

## 2026-04
- 2026-04-24 管理画面にサーバのデプロイ日時を表示 Phase 2: `/admin/` システム情報ページの「サーバー」セクションに「デプロイ日時」行を稼働時間の直下に追加（`web/admin/app.js` の `renderSystem()` で `s.deployedAt` が truthy なら `new Date(s.deployedAt).toLocaleString('ja-JP')` を `escapeHtml` 経由で表示、falsy なら muted 色 `#94a3b8` / font-weight 400 で「未設定」を表示、`cd server && npx tsc --noEmit` 緑）
- 2026-04-24 管理画面にサーバのデプロイ日時を表示 Phase 1: サーバ側実装（`server/.env.example` に `DEPLOYED_AT` のコメント付きテンプレート行を追加、`server/src/services/admin-service.ts` の `getSystemInfo()` 戻り値に `deployedAt: string | null` を追加しヘルパ `getDeployedAt()` で `process.env.DEPLOYED_AT` を読み空文字/undefined/`Date.parse` が NaN のときは `null` を返し、バリデーションを通った文字列はそのまま返す（再整形しない）、`server/tests/unit/admin-service.test.ts` を新設し `setupTestDatabase()` を取り回して `beforeEach`/`afterEach` で `DEPLOYED_AT` をクリーンに管理しつつ「未設定→null」「有効な ISO 8601→文字列をそのまま返す」「`'not-a-date'`→null」の 3 ケースをカバー、`cd server && npm test`（14 files / 136 tests）が緑）
- 2026-04-24 本番サーバで `/api/ai/suggest` が未ログイン時に `DEVICE_ID_SECRET が設定されていません` で 500 落ちしていた件を修正（原因は本番コンテナへ `server/.env` の envs が渡っておらず `DEVICE_ID_SECRET` 未設定のため `rate-limit-ai.ts` の `hashDeviceId` が throw していた。デプロイシステム側で env_file から注入するように直し、リポジトリからは `docker-compose.yml` を削除してデプロイ定義の単一ソース化）
- 2026-04-21 管理画面で docs/plans, docs/specs のマークダウンドキュメントを閲覧可能に（API追加 + 一覧/閲覧画面）
- 2026-04-21 dev-admin Step1: dev-admin/ ディレクトリ作成、package.json / tsconfig.json 作成
- 2026-04-21 dev-admin Step2: Express サーバ実装（/api/docs, /api/docs/:category/:file, /api/design/:file、ポート 3010、127.0.0.1 バインド）
- 2026-04-21 dev-admin Step3: dev-admin/src/web/ に index.html / style.css / app.js を作成（サイドバー + markdown 表示 + design iframe プレビュー）
- 2026-04-21 dev-admin Step4: ルートに dev-admin.sh 起動スクリプトを追加
- 2026-04-21 dev-admin Step5: 動作確認（/api/docs で plans / specs / design 一覧、/api/docs/:category/:file の markdown→HTML 変換、/api/design/:file の HTML 配信、静的 Web UI、path traversal / 拡張子チェックが正しく動作）
- 2026-04-21 dev-admin Step6: 既存 admin からドキュメント関連機能を削除（server/src/routes/admin.ts の /docs-files エンドポイント・関連コード・未使用 import、web/admin/app.js の renderDocs / renderDocFile / DOC_CATEGORY_LABELS / doc-file ルーター、web/admin/index.html のサイドバー「企画ドキュメント」項目）
- 2026-04-21 dev-admin Step7: CLAUDE.md に dev-admin の起動方法を追記（./dev-admin.sh、ポート 3010、DEV_ADMIN_PORT での上書き）
- 2026-04-21 dev-admin レイアウト変更 Step1: サーバ `/api/docs` をツリー構造レスポンス（`{files, dirs}`）に変更、`listTree` 再帰実装、md ファイル取得をカテゴリ配下のサブディレクトリまで再帰探索に対応
- 2026-04-21 dev-admin レイアウト変更 Step2: `dev-admin/src/web/index.html` にトップタブ DOM（Plans / Specs）を追加
- 2026-04-21 dev-admin レイアウト変更 Step3: `app.js` にタブ切替・ツリー描画（再帰）・折りたたみ・localStorage 連携・旧 `#design/` ハッシュの `#specs/design/` リダイレクトを実装
- 2026-04-21 dev-admin レイアウト変更 Step4: `style.css` にトップタブ・ツリーのインデント・トグルアイコンのスタイルを追加
- 2026-04-21 dev-admin レイアウト変更 Step5: 動作確認（タブ切替でサイドバー差し替え / `specs/design/` の折りたたみ / タブ・折りたたみ状態の localStorage 復元 / 既存 `#plans/xxx.md` 等のハッシュ互換 / 旧 `#design/xxx.html` → `#specs/design/xxx.html` リダイレクト / サーバ側 API レスポンス・エラーハンドリング）
- 2026-04-21 dev-admin 画面全体を上下2段レイアウトに変更（上段: `dev-admin` ブランド + Plans/Specs タブ + ページタイトル + パス、下段: サイドバーのファイルツリー + コンテンツ。トップバーをフル幅に昇格、サイドバーは固定配置から flex レイアウト内のスクロール領域に変更）
- 2026-04-21 dev-admin を GitHub 風ライトテーマに再配色（サイドバーを角丸ピル化、アクティブタブをオレンジ下線に、インデントを margin ベースに変更）
- 2026-04-21 plans の md ドキュメントに「アーカイブする」ボタンを追加（`docs/plans/<file>.md` → `docs/plans/archive/<file>.md` に移動する `POST /api/docs/plans/:file/archive` エンドポイントを追加、`docs/plans/archive/` ディレクトリを用意）
- 2026-04-21 ログインなしで使えるようにする: 変更プラン作成（`docs/plans/no-login-mode.md` に設計検討を、`docs/plans/no-login-mode-impl.md` に確定実装プラン（Phase 1-5 分割・API 最終形・リスク整理）を追加）
- 2026-04-22 dev-admin TODO エディタ Phase 1: サーバ側 API（`EDITABLE_FILES` ホワイトリスト追加、`GET /api/files/:name` / `GET /api/files/:name/render` / `PUT /api/files/:name` 実装、mtime 楽観ロックで 409、tmp→rename のアトミック書き込み、curl で 200/409/400/404 動作確認）
- 2026-04-22 dev-admin TODO エディタ Phase 2: SSE 変更通知（`GET /api/files/watch` を `text/event-stream` で実装、`fs.watch` + `stat` による mtime 比較重複排除、2 秒ポーリングのフォールバック・30 秒 keep-alive ping・切断時 watcher 解除、`/api/files/:name` より前にマウントしてルート衝突を回避。curl -N と `echo >>` で TODO.md / DONE.md の変更通知を確認）
- 2026-04-22 dev-admin TODO エディタ Phase 3: クライアント UI 閲覧（topbar に「TODO」タブを追加・`CATEGORIES` 拡張、`renderTodoView()` で左 TODO/DONE リンク・右プレビューのレイアウトを実装、`GET /api/files/:name/render` の HTML を注入。TODO タブで TODO.md / DONE.md が整形表示されることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 4: クライアント UI 編集（プレビュー/編集のサブタブ + textarea + 保存/破棄ボタン、`PUT /api/files/:name` + `baseMtime` での保存と 409 時のダイアログ[リロード / 維持 / 強制上書き]、`isDirty` 管理と `beforeunload` 離脱警告、保存成功トーストを実装。編集・保存で実ファイルが更新され、外部変更時は 409 ダイアログが出ることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 5: リアルタイム反映と競合表示（`EventSource('/api/files/watch')` を init で接続、`onopen/onerror` で topbar の切断インジケータを制御、`change` 受信時にモード別分岐: プレビューは自動リフレッシュ + 「外部で更新されました」バッジ、編集 clean は textarea と mtime 差し替え + 情報バー通知、編集 dirty は黄色警告バー + `[差分を見る]`（手元 vs 外部の 2 ペイン）/`[外部版を読み込む]`/`[このまま編集を続ける]` の 3 アクション。多層サインとして競合中はサイドバーに赤●バッジ・タブタイトルに `(!) ` を prepend。保存直後の自己書き込みを `selfWrittenMtimes` / `saveInFlight` で除外。curl -N で外部編集時の SSE 通知を確認）
- 2026-04-22 dev-admin TODO エディタ Phase 5b: 手動再取得（TODO ビューのツールバーに `↻ 再取得` ボタンを常設、tooltip に最終取得時刻を表示・未反映の外部更新があるときはボタンを色強調、プレビュー / clean 編集時はトースト付きで再取得・dirty 編集時は confirm で破棄確認、`R` 単独キーショートカット（textarea/input 非フォーカス時のみ、Cmd/Ctrl+R はブラウザリロードに譲る）を実装。SSE 切断状態でも再取得ボタン経由で最新内容を取得できることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 6: 仕上げ（style.css に TODO ビュー用スタイル [todo-editor 等幅 textarea、トースト、モーダル、差分 2 ペイン、SSE 切断インジケータ、外部更新バッジ、情報バー、黄色競合バー、サイドバー赤●バッジ、再取得ボタン emphasized] が網羅されていることを確認、CLAUDE.md の dev-admin セクションに TODO 編集・SSE リアルタイム反映・楽観ロック・再取得 UI を追記、回帰確認として Plans/Specs の `/api/docs` ツリー・`/api/docs/plans/:file.md` レンダリング・`/api/docs/specs/:file.md` レンダリング・archive ディレクトリの列挙・TODO.md の GET / render / watch エンドポイントが全て 200 で動作することを確認）
- 2026-04-22 テスト導入 Phase 1: サーバ側テスト基盤（vitest / @vitest/coverage-v8 / supertest / @types/supertest を server/ に導入、`server/vitest.config.ts`（`environment: 'node'`・`pool: 'forks'`・`setupFiles`）作成、`package.json` に `test` / `test:watch` / `test:coverage` 追加、`src/app.ts` に `createApp()` を切り出して `src/index.ts` は `listen` のみに薄型化、`tests/setup.ts` で `JWT_SECRET` / 外部 API キー / `DB_PATH=/tmp/cb-test-<pid>.db` を強制・本体 `shopping.db` 防衛ガード、`tests/helpers/db.ts`（`initDatabase` + `truncateAllTables` + `closeDatabase`）と `tests/helpers/auth.ts`（`createTestUser` / `createAuthHeader`）・`tests/helpers/app.ts` を整備、`database.ts` に `closeDatabase()` をテスト用途で追加、スモークテスト `tests/integration/health.test.ts` を追加して `npm test` が緑・`npm run build` も通ることを確認、本体 `shopping.db` の mtime/size が変化しないことを確認）
- 2026-04-22 テスト導入 Phase 3: サーバ route 層統合テスト（`tests/integration/` 配下に 4 本を追加、計 102 tests 緑。`auth.test.ts` = `vi.mock('resend')` で Resend クラスをモック化し `/api/auth/login` → `/api/auth/verify-code` の 2 ステップ（Resend の `to`/`text` からコードを回収 → verify で JWT 取得 → `split('.')` が 3 要素）、email の trim/lowercase、login の 400（missing / no @）、verify の 400/401（missing / unknown / wrong code / expired / 再利用拒否）、`/api/auth/me`（Bearer 無し 401・無効トークン 401・正常時は userId+email 返却）、`/api/auth/google-client-id` の結線確認。`shopping.test.ts` = 認証欠落系（no header / malformed / bad Bearer = 401）、POST の 201 と 400（空/空白）と trim、GET のユーザースコープ、PUT の checked 反映/404（存在しない・他ユーザー）、DELETE の 200/404、`DELETE /api/shopping/checked` の bulk 削除件数、`PUT /api/shopping/reorder` の並び替えと 400（非配列）、`GET /api/shopping/suggestions` の除外挙動と `?q=` 前方一致。`dishes.test.ts` = 認証 401、CRUD（POST 201・400・GET スコープ・PUT rename/400/404・DELETE が soft-delete で GET から消える/404）、食材リンク（POST 200 後に items に反映 / 400 itemId 欠落 / DELETE unlink / 404 未リンク）、`PUT /api/dishes/reorder` と `PUT /api/dishes/:id/items/reorder` の並び替え + 400、`GET /api/dishes/suggestions` が削除済み（active=0）料理名を返す。Gemini 呼び出しを含む `/:id/suggest-ingredients` は no-login 移行で廃止予定のため対象外（plan 通り）。`saved-recipes.test.ts` = 認証 401、POST 201（steps/ingredients が JSON 文字列で保存）・400（dishName/title 欠落）、GET 一覧のユーザースコープ、GET 個別の 200/404/他ユーザー 404、PUT `/like` のトグル挙動（1↔0・like_count）と 404、複数ユーザーの like 集計（3 人で like_count=3）、GET `/shared` が like>0 のみ返し `liked` が viewer 視点を反映（Alice 視点 0・Bob 視点 1）、未 like 時は空配列、DELETE の 200/404。カバレッジ: `shopping.ts` 97.67%・`saved-recipes.ts` 87.23%・`auth.ts` 70%（Google OAuth 経路は検証不能で意図的に除外）・`dishes.ts` 56.61%（未カバー行の大半は plan で除外した `/suggest-ingredients`）。`src/app.ts` 90.62% / `src/database.ts` 100%）
- 2026-04-22 テスト導入 Phase 4: モバイル側テスト基盤（`mobile/` に jest / jest-expo@~54 / @types/jest / ts-jest を devDependency として導入、`babel.config.js`（`babel-preset-expo`）を新設、`jest.config.js` で `jest-expo/jest-preset` を展開し `testMatch: __tests__/**/*.test.{ts,tsx}` と `setupFiles` を結合、`jest.setup.ts` で `expo-secure-store` をインメモリ Map モック化（`__reset` ヘルパ付き）、`package.json` に `test: jest` を追加。テストは 4 ファイル / 16 tests が緑: `__tests__/smoke.test.ts`（bootstrapping 確認）、`__tests__/stores/shopping-store.test.ts`（`api/shopping` と `api/dishes` を `jest.mock`、`addItem` が createItem→loadAll 経由で state を反映、`toggleCheck` が `items` と `dishes[].items` の両方を楽観更新し無関係な item には触れない、`reorderItems` が orderedIds を api にそのまま渡す、`deleteItem` が state と dish 配下の入れ子 items から対象 id を除去しつつ loadAll を呼ばない）、`__tests__/stores/auth-store.test.ts`（`login` は requestLogin 経由だけで isAuthenticated を変えない、`verify` が JWT を SecureStore に保存し state を authenticated に更新、`logout` が token 削除＋state リセット、`checkAuth` の 3 パス＝トークン無し・getMe 成功・getMe 失敗で `isLoading` が確実に false 化）、`__tests__/api/client.test.ts`（axios `interceptors.request.handlers` を直接呼び出し、token 保存時に `Authorization: Bearer <token>` が付与／未保存時は未設定のまま、`interceptors.response.handlers.rejected` を呼び出し 401 時に `SecureStore.deleteItemAsync` が走り 500 など他ステータスでは token が残る）。`cd mobile && npm test` が 1s 台で緑）
- 2026-04-22 テスト導入 Phase 2: サーバ service 層ユニットテスト（`tests/unit/` 配下に 4 本を追加、計 41 tests 緑。`shopping-service.test.ts` = createItem/getAllItems の userId スコープ・position シフト・category 既定値、updateItem で checked 0→1 時のみ purchase_history 記録（重複なし）、他ユーザー item の更新拒否、deleteCheckedItems の件数返却、getSuggestions が未チェック shopping_items を除外しチェック済みは候補に残す・前方一致・case-insensitive・ユーザー横断しない。`dish-service.test.ts` = createDish→linkItemToDish→unlinkItemFromDish→deleteDish のライフサイクル（deleteDish は active=0 のソフト削除、リンクしていた shopping_item は dish_id=NULL で残存）、他ユーザー item をリンク不可、reorderDishes / reorderDishItems が position を 0 起点で振り直し他ユーザーに影響しない。`saved-recipe-service.test.ts` = create / getAllSavedRecipes / getSavedRecipe の owner スコープ、deleteSavedRecipe の権限制御、toggleLike の trivial なトグルと複数ユーザーの like_count 集計、getSharedRecipes が like>0 のみ返し liked を viewer 視点で反映し like_count desc で並ぶ。`auth-service.test.ts` = `vi.mock('resend')` で Resend をクラスモック化しネットワーク遮断、findOrCreateUser の idempotency、createMagicLinkToken→verifyOtpCode→generateJwt の 3 ステップ（6 桁コード / 64 hex トークン / JWT payload 検証）、使用済みトークンの再利用拒否・新トークン発行時に旧トークン無効化・期限切れトークン検出&mark used、last_login_at 更新、cleanupExpiredTokens が使用済み＋期限切れを削除し有効な未使用のみ残す、sendOtpEmail が Resend.emails.send を 1 回だけ呼び宛先とコードを載せる。`npm test` で 5 files / 41 tests が緑）
- 2026-04-22 テスト導入 Phase 5: CI 連携（`.github/workflows/test.yml` を新規作成。`push` を `main` に、`pull_request` を全ブランチでトリガ。`strategy.matrix.project: [server, mobile]` で並列 2 ジョブ・`fail-fast: false`・`defaults.run.working-directory: ${{ matrix.project }}`。各ジョブは `actions/checkout@v4` → `actions/setup-node@v4`（Node 20・`cache: npm`・`cache-dependency-path: ${{ matrix.project }}/package-lock.json`）→ `npm ci` → `npm test`。better-sqlite3 の native build は Node 20 での `npm ci` でカバー。ローカルで `cd server && npm test`（9 files / 102 tests）と `cd mobile && npm test`（4 files / 16 tests）が緑を再確認。README 先頭に `test` ワークフローのバッジを追加し、末尾に「## テスト」セクション（`server` / `mobile` のコマンド・CI 動作・`docs/plans/testing.md` への誘導）を追記）
- 2026-04-22 ログインなしで使えるようにする Phase 1: サーバ側基盤（`server/src/database.ts` に `ai_quota(key,date,count)` テーブル追加、`server/src/middleware/auth.ts` に `optionalAuth` 追加、`server/src/middleware/rate-limit-ai.ts` を新規作成し `req.userId` 有無で `user:<id>`/`device:<sha256(raw+DEVICE_ID_SECRET)>` に分岐して JST 日次 UPSERT・上限超過で `429 ai_quota_exceeded`・成功時に `X-AI-Remaining` ヘッダ付与、`server/src/routes/ai.ts` 新設 `POST /api/ai/suggest` + プロンプト/パース処理を `server/src/services/dish-ai.ts` に分離、`server/src/routes/saved-recipes.ts` に `POST /bulk` 追加と `savedRecipesSharedRouter`（`optionalAuth` で `liked=0` 分岐）を切り出し、`server/src/services/saved-recipe-service.ts` の `getSharedRecipes` を userId optional 化・`createSavedRecipesBulk` 追加、`server/src/routes/dishes.ts` から `/:id/suggest-ingredients` ハンドラを削除し `PUT /:id/ai-cache` を新設、`server/src/routes/migrate.ts` 新設（body: items/dishes/savedRecipes、トランザクションで一括 INSERT、localId→サーバ ID の 3 マップを返却）、`server/src/app.ts` で `/api/ai` を `optionalAuth`+`rateLimitAi` で、`/api/saved-recipes/shared` を認証ガードなしで先行マウントし `/api/migrate` は `requireAuth`、廃止した `server/src/routes/claude.ts`・`recipes.ts`・`services/claude-service.ts` を削除、`.env`/`.env.example` に `DEVICE_ID_SECRET`/`AI_LIMIT_USER=20`/`AI_LIMIT_GUEST=3` を追加、`tests/setup.ts` に同環境変数を固定、`tests/integration/ai.test.ts`（Gemini を `vi.mock` でスタブ、guest 200＋`X-AI-Remaining=2`・X-Device-Id 欠落 400・3 回連続後 429＋`resetAt`・別デバイスで独立・ログイン時 20 回・dishName 欠落/空白 400 の 6 ケース）と `tests/integration/migrate.test.ts`（401・import→id マップ＋サーバ反映＋ dish/item リンク復元・空 body・ユーザー分離）を新設、`tests/integration/saved-recipes.test.ts` に `POST /bulk`（201＋400×2）と `GET /shared` 未ログインパスを追加、`tests/integration/dishes.test.ts` に `PUT /:id/ai-cache` の 200/400/404 を追加、`docs/specs/shopping-list.md` から `/api/recipes/recommend`・`/:id/suggest-ingredients` 節を削除し `/api/ai/suggest`＋`/ai-cache` を記載・廃止 API 注記を追加、`npm run build`・`npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 1 動作確認: curl でサーバ単体動作確認（未ログイン `POST /api/ai/suggest` のハッピーパス＋`X-AI-Remaining` 減算・3 回到達で 429＋`resetAt`・`X-Device-Id` 欠落で 400・`dishName` 空で 400、ログイン時 `X-AI-Remaining: 19` 起点、`GET /api/saved-recipes/shared` が未ログインでも 200 かつ `liked` は常に 0、`POST /api/migrate` が 201 で `dishIdMap`/`itemIdMap`/`savedRecipeIdMap` を返し `savedRecipes.source_dish_id` が解決済み ID を指す・未認証で 401、廃止ルート `/api/claude`・`/api/recipes/recommend`・`POST /api/dishes/:id/suggest-ingredients` が 404、`npm test` 緑）
- 2026-04-22 ログインなしで使えるようにする Phase 2 動作確認: 新規起動（未ログイン）でタブがそのまま開けること、☰メニューの「ログイン」から AuthModal が出て メール → コードの 2 ステップで認証成功し、成功時にモーダルが自動で閉じて☰メニューが `<email>` + 「ログアウト」表示に切り替わることを実機確認
- 2026-04-22 ログインなしで使えるようにする Phase 2: モバイル認証 UI 差し替え（`mobile/` に `expo-application` / `expo-crypto` を `npx expo install` で導入、`mobile/src/utils/device-id.ts` 新設（iOS は `Application.getIosIdForVendorAsync()`、Android は `Application.getAndroidId()`、どちらも null のときは `Crypto.randomUUID()` を `expo-secure-store` にキャッシュ、生 ID をそのまま返しハッシュ化はサーバに委譲、モジュールスコープにメモキャッシュ＋テスト用 `__resetDeviceIdCacheForTests`）、`mobile/src/api/client.ts` の request interceptor で JWT が無いときのみ `X-Device-Id` を付与（失敗は best-effort で無視）、`mobile/src/stores/auth-store.ts` を拡張して `authModalVisible`/`authModalReason`/`authModalOnSuccess` を追加、`requestLogin({ reason, onSuccess })`/`closeAuthModal()` を新設、既存 `login(email)` はインポート名衝突を避けて `sendMagicCode(email)` にリネーム、`verify` は成功時に保存した `authModalOnSuccess` を呼びつつ modal を閉じる、`mobile/src/components/auth/AuthModal.tsx` 新設（メール→コードの 2 ステップ、`KeyboardAvoidingView` + transparent `Modal`、reason ラベル表示、ローディング中はボタンを `ActivityIndicator` 化、キャンセル/別アドレス リンク付き）、`mobile/app/_layout.tsx` から `segments`/`useRouter` ベースの未認証リダイレクトを撤去し `<Slot>` の兄弟として `<AuthModal>` を常設、`mobile/app/(auth)/` 配下（`_layout.tsx`/`login.tsx`/`verify-code.tsx`）を削除、`mobile/app/(tabs)/_layout.tsx` の☰メニューを `isAuthenticated` で分岐しゲスト時は「ログイン」、ログイン時は `<email>` + 「ログアウト」を表示、`mobile/jest.setup.ts` に `expo-application`/`expo-crypto` のモックを追加、`mobile/__tests__/stores/auth-store.test.ts` を `sendMagicCode` 対応に刷新し `requestLogin`/`closeAuthModal`/`verify` 時の onSuccess 発火・modal クリアまでカバー、`mobile/__tests__/api/client.test.ts` に JWT 無しで `X-Device-Id` が付与されるケースを追加、`mobile/__tests__/utils/device-id.test.ts` を新設（iOS IDFV・Android SSAID・null→UUID フォールバック・既存保存 UUID の再利用・モジュール内メモキャッシュの 5 ケース）、`npm test`（5 files / 24 tests）と `npx tsc --noEmit` が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 4: マイグレーション（`mobile/src/api/migrate.ts` 新設 `migrate({items, dishes, savedRecipes})` で `POST /api/migrate` を叩き `dishIdMap`/`itemIdMap`/`savedRecipeIdMap` を受け取る型付きラッパ、`mobile/src/utils/migration.ts` 新設 `runLoginMigration()` が `useShoppingStore` + `useRecipeStore` のローカルデータを集計し空なら黙って `setMode('server')`+`loadAll()`、非空なら `Alert.alert` で「移す / 破棄 / キャンセル」を問い合わせ、破棄時は二段目の警告ダイアログで確認後にローカル破棄、移行時は `items.id`→`localId`・`dish_id`→`dishLocalId`・`source_dish_id`→`sourceDishLocalId` にマッピングして JSON カラムをパースしながら送信、マイグレ例外時は Alert を出して `cancelled` を返す、`mobile/src/stores/auth-store.ts` の `verify` を「トークン保存＋isAuthenticated 更新」だけに薄型化しモーダル閉じ／onSuccess 発火の責務を `finishLogin`/`cancelLogin` アクションに分離、`logout` は `removeToken` に加えて `useShoppingStore.clearLocalData/setMode('local')` と `useRecipeStore.clearLocalData/setMode('local')` を呼んで前ユーザーのサーバデータを完全にパージ、`mobile/src/components/auth/AuthModal.tsx` の `handleVerify` を `verify` → `runLoginMigration()` → 結果に応じて `finishLogin`/`cancelLogin` を呼ぶフローに変更、`mobile/__tests__/stores/auth-store.test.ts` を `verify`/`finishLogin`/`cancelLogin`/`logout`（ローカルストアクリアを含む）に分割した 10 テストに再構成、`mobile/__tests__/utils/migration.test.ts` 新設で「空ローカル→プロンプト無し `migrated`」「移すで `migrate()` 呼び出しペイロード検証」「破棄＋二段目確認で `discarded`＋ローカルクリア」「キャンセルで `cancelled`＋ローカル保持」「migrate 失敗で `cancelled`＋ローカル保持」の 5 ケースを網羅、`cd mobile && npm test`（9 files / 56 tests）と `cd mobile && npx tsc --noEmit`、`cd server && npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 3: モバイルローカルストア化（`@react-native-async-storage/async-storage` を `npx expo install` で導入、`mobile/src/api/ai.ts` 新設で `suggestAi(dishName, extraIngredients?)` を実装し `x-ai-remaining` をレスポンスヘッダから抽出・429 `ai_quota_exceeded` を `AiQuotaError(resetAt)` クラスに変換、`mobile/src/api/dishes.ts` から `suggestIngredients()` を削除して `updateDishAiCache(dishId, ingredients, recipes)` に差し替え、`mobile/src/api/saved-recipes.ts` に `createSavedRecipesBulk(recipes)` を追加、`mobile/src/stores/ai-store.ts` 新設（`remaining`/`quotaExceeded`/`resetAt` state、`setRemaining`/`markQuotaExceeded`/`reset`）、`mobile/src/stores/shopping-store.ts` を zustand `persist` + AsyncStorage で包んで `mode: 'local' | 'server'`/`nextLocalId`（負の連番）を追加、全メソッドに local/server 分岐を入れ `addItem`/`addDish`/`linkItemToDish`/`toggleCheck`/`deleteItem`/`deleteCheckedItems`/`reorderItems`/`reorderDishes`/`reorderDishItems`/`updateItemName`/`updateDish`/`deleteDish` が local モードでは AsyncStorage 経由、server モードでは既存 API 経由で動くように、`suggestIngredients(dishId, extras?)` は `suggestAi` 呼出→`AiQuotaError` 時は `useAiStore.markQuotaExceeded`→成功時は `setRemaining`→dish の `ingredients_json`/`recipes_json` にキャッシュ→server モードでは `updateDishAiCache` も呼ぶ→`useRecipeStore.autoSaveRecipes` で自動保存→`recipeStates` を返却、`partialize` で local 時のみ items/dishes を永続化、`mobile/src/stores/recipe-store.ts` を `persist` + AsyncStorage で包んで `mode`/`nextLocalId` 追加、`autoSaveRecipes(dishName, recipes, sourceDishId)` が local では負 ID 採番・server では `POST /api/saved-recipes/bulk`、`toggleLike` は local モードで `useAuthStore.requestLogin` を呼んで AuthModal に誘導、`loadSharedRecipes` は未ログインでもサーバ経路で取得、`loadSavedRecipes` は local モードでは永続化された state を使うので no-op、`mobile/src/components/dishes/IngredientsScreen.tsx` を新 `suggestIngredients` フローに差し替え、前回キャッシュがあれば初期表示に使い無ければ AI 呼出、残回数ボタン文言「レシピを再検索（残り X 回）」、`AiQuotaError` 時は未ログイン→`requestLogin(reason, onSuccess=retry)`/ログイン済→「本日の上限に達しました」アラート、`mobile/app/(tabs)/shared.tsx` は未ログインでもレシピ閲覧可・いいね押下で `requestLogin`、`mobile/app/(tabs)/recipes.tsx` は local モードでは全 saved-recipes を表示（server モードのみ liked でフィルタ）・いいねは `requestLogin`、`mobile/app/_layout.tsx` に `isAuthenticated` と store mode を同期する effect を追加（認証成功で `server` に切替＋`loadAll`/`loadSavedRecipes`、ログアウトで `local` に戻す）、`mobile/jest.setup.ts` に AsyncStorage の in-memory モックを追加、`__tests__/stores/shopping-store.test.ts` を刷新し server モードの既存 4 ケース＋local モード 6 ケース（addItem で負 ID 採番・linkItemToDish でローカル紐付け・toggleCheck/deleteItem/deleteCheckedItems がローカル完結・suggestIngredients が ai/suggest→dish キャッシュ→local saved-recipes 自動保存＋`setRemaining`・`AiQuotaError` で `markQuotaExceeded`）＋setMode 2 ケースを網羅、`__tests__/stores/ai-store.test.ts` 新設（setRemaining の n<=0 で quotaExceeded 切替・null 時・markQuotaExceeded・reset）、`__tests__/stores/recipe-store.test.ts` 新設（server: loadSavedRecipes/toggleLike/autoSaveRecipes、local: loadSavedRecipes が no-op・autoSaveRecipes が負 ID 採番・toggleLike で requestLogin・deleteSavedRecipe がローカルのみ・setMode で state クリア）、`__tests__/api/ai.test.ts` 新設（ingredients/recipes/X-AI-Remaining 抽出・ヘッダ欠落で remaining=null・429 で AiQuotaError・他エラーは再 throw・extraIngredients 転送）、`npm test`（8 files / 49 tests）と `npx tsc --noEmit` が緑）
- 2026-04-23 実機アプリの接続先サーバ切り替え Phase 1: API URL を環境変数化（`mobile/src/config/api-endpoint.ts` を新設し `process.env.EXPO_PUBLIC_API_URL ?? 'https://basket.chobi.me'` を export、`mobile/src/api/client.ts` の `axios.create` の `baseURL` ハードコードを `API_BASE_URL` 参照に差し替え、`cd mobile && npm test`（9 files / 56 tests）が緑）
- 2026-04-23 実機アプリの接続先サーバ切り替え Phase 2: ビルドプロファイル別の env とローカル HTTP 許可（`mobile/eas.json` の `development` / `preview` プロファイルに `"env": { "EXPO_PUBLIC_API_URL": "http://192.168.x.x:3000" }` を追加・`production` は env 未指定のままフォールバックで本番 URL、`mobile/app.config.ts` を新設して `app.json` を `ConfigContext.config` 経由で継承し `process.env.EAS_BUILD_PROFILE === 'development' | 'preview'` のときだけ iOS `infoPlist.NSAppTransportSecurity.NSAllowsArbitraryLoads = true` と Android 側のクリアテキスト許可用プラグイン `["expo-build-properties", { android: { usesCleartextTraffic: true } }]` を plugins に追記、`mobile/` に `expo-build-properties@~1.0.10` を `npx expo install`、`mobile/.env.example` 新規作成（`EXPO_PUBLIC_API_URL` の書き方と未設定時の本番フォールバックを明記）、`mobile/.gitignore` に `.env` を追加、`npx expo config --type public` を profile 無し/`EAS_BUILD_PROFILE=preview` の両方で検証し production 側では ATS/cleartext 設定が混入しないこと・preview 側では両方が付与されることを確認、`cd mobile && npm test`（9 files / 56 tests）が緑）
- 2026-04-23 本番サーバログを外部から安全に閲覧 Phase 3: 管理 API 追加（`server/src/services/logs-service.ts` を新設し、`process.env.LOG_FILE_PATH` を起点に pino-roll の出力ファイル（`server.YYYY-MM-DD.N.log`）を最新 mtime で解決する `findActiveLogFile()`、`readRecentLogs(lines, filter)`（全行読み → JSON parse → `level >= threshold` と `JSON.stringify(entry).toLowerCase().includes(q)` でフィルタ → 末尾 N 件）、`tailLogFile(filter, onEntry)`（初期位置＝現在のファイルサイズ、1 秒ポーリング＋ディレクトリの `fs.watch` で `size > position` の差分を `createReadStream({start, end})` で読み JSON Lines に分解、日次ローテで active ファイル名が変わったら `position=0` でリセット、返り値で watcher + interval を破棄）を公開、`server/src/routes/admin.ts` に `GET /api/admin/logs?lines=200&level=info&q=` を追加（lines は 1..2000 にクランプ、デフォルト 200）、`GET /api/admin/logs/stream` を追加（`Content-Type: text/event-stream; charset=utf-8` / `Cache-Control: no-cache, no-transform` / `Connection: keep-alive` / `X-Accel-Buffering: no` を送出し `res.flushHeaders()` → 直近 50 件を `data: {JSON}\n\n` で吐いてから tail を開始、30 秒ごとに `: keep-alive\n\n` のハートビート、`req.on('close' | 'error')` で heartbeat の clearInterval と tail の unwatch を実行）、`server/tests/integration/admin.test.ts` を新設し未ログイン 401・非 admin 403・recent 取得（`msg` 配列の一致）・`?lines=` 末尾 N 件・`?level=warn` 閾値フィルタ・`?q=BAN` で case-insensitive 部分一致・非 JSON 行の黙殺・pino-roll の `server.2026-04-23.1.log` 解決・`LOG_FILE_PATH` 未設定で空配列、の 9 ケースと SSE smoke（`http.request` で `app.listen(0)` に接続 → `preexisting` を受信 → `fs.appendFileSync` で 1 行追記 → `appended` を受信 → `req.destroy()`）＋ SSE 非 admin 403、計 11 tests を追加、`cd server && npm test`（12 files / 130 tests）と `npx tsc --noEmit` が緑）
- 2026-04-23 本番サーバログを外部から安全に閲覧 Phase 2: ログファイル出力とローテーション（`server/` に `pino-roll@^4` を `npm install` で追加、`server/src/lib/logger.ts` を `pino.transport` のマルチターゲット構成に変更し `LOG_FILE_PATH` 環境変数が指定された場合のみ stdout (`pino/file` → fd=1) に加えてファイル出力を有効化・未指定時は従来の同期 stdout pino のまま（テスト・ローカル開発を壊さない）、`pino-roll` の `frequency: 'daily'` + `dateFormat: 'yyyy-MM-dd'` + `limit: { count: 6 }`（active 1 + rotated 6 = 最大 7 ファイル）で日次ローテ、起動時に `fs.mkdirSync(path.dirname(LOG_FILE_PATH), { recursive: true })` で親ディレクトリを先行作成してパーミッションエラーを早期顕在化（pino-roll 側にも `mkdir: true` を渡して二重防御）、`redact` 設定は Phase 1 そのまま維持、`docker-compose.yml` の `environment` に `LOG_FILE_PATH=/app/data/logs/server.log` を追加し既存 volume `./data:/app/data` で自動永続化、`.gitignore` に `data/logs/` を追記（要求メタデータを含むログをコミットしない）、`LOG_FILE_PATH=/tmp/cb-log-smoke/server.log` でスモーク実行し stdout とファイルの両方に同一 JSON Lines が書かれること・`body.otp` が `[REDACTED]` 置換されること・ファイル名が `server.2026-04-23.1.log` となり pino-roll が翌日以降自動で `server.2026-04-24.1.log` を作る仕様であることを確認、`cd server && npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 5: 仕上げ（`mobile/app/(tabs)/_layout.tsx` の☰メニューに `useAiStore.remaining` を「AI 残り X 回」として表示（未ログイン時はログイン項目との区切り線付き、ログイン時は email の下に並べる）、`web/privacy.html` を更新し「1. 収集する情報」にログイン/未ログインの保存先の違いと端末識別子ハッシュ値の項目を追加、新設「4. 端末識別子の取り扱い」で iOS IDFV / Android SSAID を `sha256(raw + DEVICE_ID_SECRET)` でハッシュ化して日次利用回数カウントにのみ使う旨・元の識別子はログ/DB に保存しない旨・広告利用しない旨・ログイン時は送信しない旨を明記、データ保存節も未ログインは端末ローカルに限る旨に修正し最終更新日を 2026 年 4 月 22 日に更新、管理画面に AI 利用状況ビューを追加（`server/src/services/admin-service.ts` に `getAiQuotaStats()` を追加し今日の JST 日付サマリ／直近 14 日の日次集計／キー単位の直近 200 件を返す、`server/src/routes/admin.ts` に `GET /api/admin/ai-quota` を追加、`web/admin/index.html` のサイドバー「システム」グループに「AI 利用状況」リンクを追加、`web/admin/app.js` の `Pages` に `'ai-quota'` を登録し `renderAiQuota()` で今日の呼出数/ユーザー利用/ゲスト利用のサマリカード・日次推移テーブル・キー単位の検索可能テーブル（ゲストはハッシュ先頭 10 桁のみ表示）をレンダリング、`server/src/services/admin-service.ts` の `getSystemInfo` が数える tableCounts に `ai_quota` を追加、ストア提出用説明（`docs/plans/archive/app-store-publish.md`）を「登録不要・ログイン不要ですぐに使い始められます」訴求に更新し未ログイン 3 回/ログイン 20 回の AI 利用回数を明記、Android の simple description も「登録不要で使える買い物リスト」に差し替え、`cd server && npm test`（11 files / 119 tests）と `cd mobile && npm test`（9 files / 56 tests）が緑）
