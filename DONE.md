# DONE

完了したタスクの記録。

<!-- 完了日 - タスク名 の形式で記録 -->

## 2026-02
- 2026-02-28 README.md 作成
- 2026-02-28 CLAUDE.md 作成
- 2026-02-28 TODO.md / DONE.md 作成
- 2026-02-28 サーバプロジェクト初期化 (package.json, tsconfig.json, .env)
- 2026-02-28 Express サーバ基盤 (エントリポイント, ヘルスチェック, CORS, エラーハンドリング)
- 2026-02-28 SQLite データベースセットアップ (shopping_items テーブル)
- 2026-02-28 買い物リスト API (CRUD: GET/POST/PUT/DELETE /api/shopping)
- 2026-02-28 Web クライアント: 買い物リスト画面 (HTML/CSS/JS)
- 2026-02-28 管理画面: レイアウト・ナビゲーション
- 2026-02-28 管理画面: データ一覧・インライン編集・削除・全件削除
- 2026-02-28 管理画面: サーバ状態モニタリング (ヘルスチェック + 統計)
- 2026-02-28 管理用 API (GET/PUT/DELETE /api/admin/shopping, GET /api/admin/stats)
- 2026-02-28 Claude Code CLI 連携 (POST /api/claude, claude-service.ts)

## 2026-03
- 2026-03-01 レシピ推薦 API (GET /api/recipes/recommend, recipes.ts)
- 2026-03-01 管理画面: おすすめレシピ UI
- 2026-03-02 リストに料理を追加できるようにし、買い物食材を料理に関連付けられるようにする
- 2026-03-02 食材・料理入力時に購入頻度が高いものをサジェスト表示する
- 2026-03-02 料理追加時にAIで具材を調べて表示し、選択することで買い物リストに追加できるようにする
- 2026-03-02 料理の具材検索はバックグラウンドで行い料理追加画面はすぐに閉じる
- 2026-03-02 料理と食材追加アイコンは縦並びから横並びに
- 2026-03-02 料理名からレシピなどの情報をAIで取得して表示する
- 2026-03-02 食材の削除ボタンを削除
- 2026-03-02 料理の情報（レシピ・具材等）を保存し、次回同じ料理を追加したときに参照できるようにする
- 2026-03-02 具材・レシピダイアログを全画面表示に変更
- 2026-03-02 料理ダイアログにAI再取得ボタンを追加
- 2026-03-02 PWA対応（ホーム画面にアイコン追加可能に）
- 2026-03-02 料理・食材の表示順を長押しドラッグで変更可能に
- 2026-03-02 食材を料理間でドラッグ移動可能に
- 2026-03-02 チェックボックスを四角から丸へ変更
- 2026-03-02 レシピの文章中の食材名をタップ可能にし、買い物リストに追加できるようにする
- 2026-03-02 AI検索中にトースト通知を表示（3秒後にフェードアウト）
- 2026-03-02 料理名を編集できるようにする（レシピページでタップ編集、変更後レシピ再取得）
- 2026-03-02 料理に特定の食材を追加してレシピを再検索できるようにする
- 2026-03-02 レシピ再検索時に指定食材以外も含む本格的なレシピが返るようプロンプト改善
- 2026-03-02 複数ユーザー対応（Magic Link 認証、JWT、ユーザーごとのデータ分離）
- 2026-03-02 食材を上下に移動したときに文字が選択状態になるのを防止
- 2026-03-02 PWA インストール用の Web ページを GitHub Pages で作成する
- 2026-03-02 管理画面を再構築（PC用ダッシュボード：ユーザー管理、全データ管理、システム監視）
- 2026-03-03 AI提案レシピを個別に保存できるようにし、レシピブック画面で閲覧・買い物リスト追加・削除可能に
- 2026-03-03 料理レシピにいいねボタンを追加（ハートアイコン、レシピ再取得時も保持）
- 2026-03-03 料理レシピページ新設（いいね数上位表示、recipe_likesテーブルで複数ユーザーいいね対応）
- 2026-03-03 ヘッダーをハンバーガーメニューに変更（メアド非表示、メニュー内にメアド+ログアウト）
- 2026-03-03 料理レシピページをいいね済みのみ表示に変更（解除時カード削除）
- 2026-03-03 料理のソフトデリート対応（削除→非表示、データ保持）
- 2026-03-03 具材モーダルのレシピにいいねアイコンが出ない問題を修正
- 2026-03-03 料理ソフトデリート時にdish_itemsリンクも削除するよう修正
- 2026-03-03 dish_items中間テーブルをshopping_items.dish_idに統合（スキーマ簡素化）
- 2026-03-03 dish_historyテーブル廃止（dishesテーブルで代替、管理画面から料理履歴ページ削除）
- 2026-03-03 管理画面の料理一覧に紐づき食材を折りたたみ表示、状態カラム追加
- 2026-03-03 料理名編集時にテキストが全選択にならないよう修正（カーソルを末尾に配置）
- 2026-03-03 サーバー再起動時にクライアントが自動リロードするバージョンチェック機能追加
- 2026-03-03 サービスのメールアドレスをGmail→Resend(noreply@chobi.me)に変更
- 2026-03-03 いいねレシピ画面を「みんなのレシピ」「自分のレシピ」に分離、ヘッダーアイコン・メニューから遷移可能に
- 2026-03-03 みんなと自分のレシピ画面に検索機能追加（タイトル・概要・手順をハイライト付きで検索）
- 2026-03-03 レシピ画面に無限スクロール追加（20件ずつ表示）
- 2026-03-03 AIプロンプトをレシピごとの食材返却に変更、レシピ固有の食材を保存
- 2026-03-03 レシピカードに「リストに追加」ボタン追加（具材モーダル・レシピページ両方）
- 2026-03-03 チェック済み食材のハイライト状態を正しく更新するよう修正
- 2026-03-03 Google認証（Sign in with Google）対応追加
- 2026-03-03 アプリ名を「お料理バスケット」に決定（管理画面に候補一覧ページ追加）
- 2026-03-03 マネタイズ検討ドキュメント作成（管理画面にページ追加）
- 2026-03-03 管理画面の企画ドキュメントを1ページに集約（アイコン候補・アプリ名・マネタイズ）
- 2026-03-03 アプリ名を「お料理バスケット」に変更（タイトル、manifest、PWAアイコン表示名など全箇所）
- 2026-03-03 ドメインを life.chobi.me → basket.chobi.me に変更（コード・ドキュメント全箇所）
- 2026-03-03 リポジトリ名を life-stream-claude → cooking-basket に変更（コード・ドキュメント全箇所）
- 2026-03-03 アプリ紹介・インストール手順ページを /about に作成（docs/index.html から移行）
- 2026-03-04 /about ページにアプリスクショ追加・特徴カードにスクショ埋め込みレイアウト採用
- 2026-03-04 デモデータ投入スクリプト作成（seed-demo.ts）
- 2026-03-04 Google認証を一時無効化（テスト完了まで）
- 2026-03-04 iPhoneアプリとAndroidアプリ開発の技術検討（管理画面にドキュメント追加）
- 2026-03-04 WSL2 + Claude Code リモート開発環境検討（管理画面にドキュメント追加）
- 2026-03-04 React Native 開発計画ドキュメント作成（管理画面にページ追加）
- 2026-03-04 Gemini 3.1 Flash-Lite対応（デフォルトモデルをgemini-3.1-flash-lite-previewに変更）
- 2026-03-04 食材/料理追加時の候補選択で即送信
- 2026-03-04 チェック済み食材を折りたたみ表示（10件ずつ追加読み込み）
- 2026-03-04 FAB横並び（料理左・食材右）+ WEB版と同じ料理アイコン
- 2026-03-04 レシピ画面: 追加素材入力廃止、追加した素材を別表示＋再検索ボタン
- 2026-03-04 タブタイトル修正（「自分のレシピ」「みんなのレシピ」）
- 2026-03-04 ログアウト機能（ヘッダーメニュー）
- 2026-03-04 料理ページに「レシピを再取得」ボタン
- 2026-03-04 追加素材（買い物リストから）を別セクションで表示 + 「この素材でレシピを再検索」ボタン
- 2026-03-04 料理名の編集可能表示（破線下線 + ✎アイコン）
- 2026-03-04 再検索ボタン統合（追加素材あり→「この素材で再検索」/ なし→「レシピを再検索」）
- 2026-03-04 料理レシピページの料理名を中央寄せ + 下線表示
- 2026-03-04 React Native Phase 6.1: 料理グループ・食材のドラッグ&ドロップ並び替え
- 2026-03-04 React Native Phase 6.2: アニメーション（既存Animated実装で十分、追加不要）
- 2026-03-04 React Native Phase 6.3: Hapticフィードバック（チェック・ドラッグ・追加・削除）
- 2026-03-04 React Native Phase 6.4: アプリアイコン設定、その他ポリッシュ完了
- 2026-03-04 買い物リストの料理・食材をドラッグで並び替え可能に（reanimated v3 + draggable-flatlist）
- 2026-03-05 「アイテム」を「食材」に名称変更（UI表示・ドキュメント・コメント全箇所）
- 2026-03-05 モバイル版: チェック済みの「すべて削除」ボタンを削除
- 2026-03-05 チェック済み食材は一定量ずつ読み込み済みであることを確認（モバイル版10件ずつ、Web版は非表示）
- 2026-03-05 モバイル版: 食材行の右側×削除ボタンを削除
- 2026-03-05 Web版: 音声入力対応（食材/料理追加モーダルにマイクボタン、Web Speech API）
- 2026-03-05 React Native アプリ Phase 1-6 全完了（初期化・認証・買い物リスト・AI具材&レシピ・レシピブック・ポリッシュ）
- 2026-03-05 EAS セットアップ完了 (eas-cli, eas login, eas init, eas.json作成)
- 2026-03-05 app.json 公開向け設定 (permissions, infoPlist, platforms)
- 2026-03-05 アイコン・スクリーンショット準備（iOS/Android両対応、adaptive-icon修正）
- 2026-03-05 プライバシーポリシーページ作成・公開 (basket.chobi.me/privacy)
- 2026-03-05 Apple Developer Program 登録 ($99/年)
- 2026-03-05 iOS App Store 審査提出（TestFlight動作確認済み）
- 2026-03-05 Google Play Console 登録 ($25)
- 2026-03-05 レシピステップのテキストはみ出し修正（RecipeCard.tsx）
- 2026-03-05 Android adaptive-icon 白背景で再生成
- 2026-03-06 Google Play クローズドテスト トラック作成・AABアップロード・審査提出

## 2026-04
- 2026-04-27 リファクタリング 2: モバイル小粒整理（M5+M6+M7+M8）。Step 1 で参照 0 件を再 grep の上 `mobile/src/components/ui/SuggestionsList.tsx`（55 行）と `mobile/src/hooks/use-debounce.ts`（12 行・空ディレクトリも削除）をファイルごと削除、`types/models.ts:45 SuggestIngredientsResponse`（`RecipeState` 含む）を削除、`stores/shopping-store.ts` の `SuggestIngredientsResult.recipeStates` フィールドと 341–355 の生成ロジックを削除（`autoSaveRecipes` の戻り値は捨てるだけに）、`__tests__/stores/shopping-store.test.ts:266` の `recipeStates` 検証も削除。Step 2 で `api/auth.ts:3-7` の独自 `ApiResponse<T>` 定義を削除して `types/api.ts` から import に統一、`type ModalMode = 'item' | 'dish' | 'edit'` を新規 `mobile/src/types/ui.ts` に集約して `app/(tabs)/index.tsx:25` と `components/shopping/AddModal.tsx:16` の重複定義を import 参照に置換、`api/saved-recipes.ts:21` / `api/migrate.ts:26` の `ingredients?: { name: string; category: string }[]` を `Ingredient[]`（`types/models.ts:29`）参照に統一。Step 3 で `mobile/src/api/client.ts` に `request<T>(method, url, body?, config?)` ヘルパを追加（`client.request<ApiResponse<T>>` を呼んで `success` 判定 + `data` 返却の 3 行を 1 関数に集約）、`api/{auth,dishes,shopping,saved-recipes,migrate}.ts` の 25 関数をアロー関数 1 行ヘルパ呼出に薄化（`shopping.deleteCheckedItems` だけは戻り値整形のため `async/await` を維持）、`api/ai.ts` の `getAiQuota` も同ヘルパに切替（`suggestAi` は `x-ai-remaining` ヘッダ参照と `AiQuotaError` マッピングがあるため `client.post` 直叩きを温存）、`__tests__/api/ai.test.ts` のモックを `request` 名前付き export 込みに更新して 3 ケース書き換え、`__tests__/stores/shopping-store.test.ts` の `mockResolvedValue(undefined)` を `Promise<null>` 戻り値に合わせて `mockResolvedValue(null)` に書き換え。Step 4 で `mobile/app/(tabs)/_layout.tsx:115` の `const { Text } = require('react-native')` を削除（冒頭 ES import 済の `Text` を使用）。`npx tsc --noEmit` 緑、Jest 11 suites / 74 tests 緑。各 Step を独立コミット（`Remove unused mobile exports and recipeStates` / `Consolidate mobile type definitions` / `Add request helper for mobile API client` / `Drop the runtime require in TabIcon`）として分離。プランは [docs/plans/archive/refactoring-mobile-cleanup.md](docs/plans/archive/refactoring-mobile-cleanup.md) に移動
- 2026-04-27 リファクタリング 1: サーバ小粒整理（migrate.ts 抽出 / docs.ts / 未使用 export / cleanup ロギング）。Step 1 で `routes/migrate.ts:42–110` の SQL 組み立て + `db.transaction(...)` を新規 `services/migrate-service.ts` の `migrateLocalData(userId, { items, dishes, savedRecipes })` に移し、route は `req.body` から 3 配列を受け取って service を呼ぶだけの薄いハンドラ（126 行 → 26 行）に置換、`tests/integration/migrate.test.ts` 4 ケース緑で route 層 SQL 直叩きを必須チェック合格に。Step 2 で `routes/docs.ts:183` の `<title>${title} - Life Stream</title>` を `お料理バスケット` に修正、245 行のインライン CSS を新規 `web/docs.css` に外出して `<link rel="stylesheet" href="/docs.css">` 参照に変更（既存 `app.use(express.static(webDir))` で配信、`/docs.css` の Content-Type が `text/css` になることを実機 curl で確認、`/docs/` / `/docs/specs/<file>` の HTML / 一覧が崩れないことも確認）。Step 3 で参照 0 件を再 grep の上、`shopping-service.ts` の `deleteAllItems` / `getUncheckedItems` / `getStats` と `saved-recipe-service.ts` の `getSavedRecipeStates` / `autoSaveRecipes`（mobile 側 `recipe-store.ts` の同名 Zustand action は別物のため影響なし）を削除（54 行減）、`database.ts:21 closeDatabase` は `tests/helpers/db.ts` から呼ばれていたため温存。Step 4 で `server/src/index.ts:13` の `try { cleanupExpiredTokens(); } catch {}` を `try { ... } catch (err) { logger.error({ err }, 'cleanup_failed'); }` に置換し本番障害を pino でログに落とす。`npx tsc --noEmit` 緑、Vitest 20 files / 216 tests 緑（pre-commit フックで Jest 11 suites / 74 tests も併走で緑）。各 Step を独立コミット（`Extract migrate route SQL` / `Externalize docs.ts CSS and fix title` / `Remove unused service exports` / `Log cleanup interval failures`）として分離。プランは [docs/plans/archive/refactoring-server-cleanup.md](docs/plans/archive/refactoring-server-cleanup.md) に移動
- 2026-04-27 リファクタリング監査（refactoring.md）。Phase 0 でガードレール / 一次データ（行数・変更頻度・`any` 件数・未使用 export・循環依存・重複リテラル）を取得し `web/admin/` の取り扱いを別プラン候補としてスコープ外確定、Phase 1 でサーバ側必須チェック（`requireAuth`/`requireCloudflareAccess` 二重掛けなし・API レスポンス形式逸脱なし・route 層 SQL 直叩きは `routes/migrate.ts` のみ・外部 API はサービス層境界に閉じている）と候補 S1〜S8（route エラー処理 + メッセージ重複、migrate route SQL 直叩き、`admin-service.ts` の `any` 集中 + 責務肥大、`database.ts` のアドホック・マイグレーション、`routes/docs.ts` のタイトル文字列 + CSS インライン、未使用 export 削除、cleanup interval ロギング）を証拠付きで列挙、Phase 2 でモバイル側必須チェック（`mobile/src/api/` から `/api/admin` 呼出なし・store と `__tests__/stores/` が 1:1 対応）と候補 M1〜M8 を抽出、Phase 3 で評価軸（メンテ性 × リスク × 工数）を回して打ち切り基準「工数 1〜2 日以下 × メンテ性中以上」に当たるものを採用、小粒は意味のある単位で束ねて 7 本の個別プラン（`refactoring-server-error-handling.md` / `refactoring-server-admin-service.md` / `refactoring-server-database-migrations.md` / `refactoring-server-cleanup.md` / `refactoring-mobile-ingredients-screen.md` / `refactoring-mobile-draggable-list.md` / `refactoring-mobile-cleanup.md`）に分解し TODO に追加、規模数日の M1（`shopping-store.ts` の local/server 二重実装）と M2（`app/(tabs)/index.tsx` の責務漏出）は TODO ストックとして残す（M1 → M2 の順で進める判断も記録）。コード変更なし、本プランは [docs/plans/archive/refactoring.md](docs/plans/archive/refactoring.md) に移動
- 2026-04-26 「みんなのレシピ」「いいね」残存記述の整理（アプリシンプル化以降のドキュメント / 紹介ページ / 管理画面のズレ修正）。Phase 1 で `README.md` の主な機能から `レシピ保存・いいね` 行を削除、ディレクトリ構成コメントの `saved-recipes.ts # レシピ保存・いいね API` / `saved-recipe-service.ts # レシピ保存・いいね管理` をそれぞれ `# レシピ保存 API` / `# レシピ保存管理` に修正、API 表「レシピ」を実装に合わせて差し替え（`GET /shared` と `PUT /:id/like` を削除、`POST /bulk` を追加）。Phase 2 で `web/about.html` の「レシピを保存・共有」フィーチャーカードを「レシピを保存」に書き換え（`feature-screenshot-row` 2 枚並びを `feature-screenshot` 1 枚に戻し `img/ss-shared.png` 参照を削除）、未使用になった `.feature-screenshot-row` の CSS ブロックも削除。Phase 3 で `web/admin/app.js` から既にサイドバー導線の無いオーファンページ群（`icon-preview` / `app-name` / `monetization` / `native-app` / `remote-dev` / `react-native-plan`）の `Pages` 定義 6 行と対応する `renderIconPreview` / `renderAppName` / `renderMonetization` / `renderNativeApp` / `renderRemoteDev` / `renderReactNativePlan` 関数 6 個を丸ごと削除（2488 → 942 行）、`node -c` で構文 OK を確認。サーバ・モバイル側のコードと DB スキーマには変更なし。プランは [docs/plans/archive/cleanup-stale-shared-likes-references.md](docs/plans/archive/cleanup-stale-shared-likes-references.md) に移動
- 2026-04-26 「自分のレシピ」タブを「レシピノート」にリネーム。Phase 1 で `mobile/app/(tabs)/_layout.tsx:58` の `title` を `'レシピノート'` に変更（タブバーラベルとヘッダタイトルが連動）、Phase 2 で `README.md` のタブ概要行（"みんなのレシピ / 自分のレシピ" → 現状の `レシピノート` 単独に整理）と API 表 `GET /api/saved-recipes` 説明（"自分のレシピ一覧" → "レシピノート一覧"）、`web/about.html:409` のスクショ alt（"自分のレシピ画面" → "レシピノート画面"）、`web/admin/app.js` 内 4 箇所（説明文・ASCII ツリー・機能説明）の "自分のレシピ" を "レシピノート" に置換。`saved_recipes` テーブル / `/api/saved-recipes` API パス / Zustand store のキー名は据え置き（UI ラベルだけ変更）。`web/admin/app.js` に残る "みんなのレシピ" 残存記述は別 TODO に切り出し。プランは [docs/plans/archive/recipes-tab-rename.md](docs/plans/archive/recipes-tab-rename.md) に移動
- 2026-04-26 料理の具材が AI 再検索で「追加具材」に降格する問題の修正。`mobile/src/components/dishes/IngredientsScreen.tsx` で AI 呼び出し用 extras を派生値 `extraIngredients` から source-of-truth な `pinnedExtras = dish.items.filter(!checked).map(name)` に切り替え（`dishItemNames` Set はこれから派生）、`handleRefresh` / `handleSearchWithExtras` を `handleSearch`（`pinnedExtras` が空でなければ extras 付き、空なら undefined）に 1 本化、3 箇所のボタン onPress（追加具材セクション・extras 0 時のフォールバック・レシピフッタ）を全て `handleSearch` に置換。これにより 2 回目以降の AI 呼び出しでも dish.items が常に extras として渡るので「春キャベツ」が AI 応答の揺らぎで「追加具材」に逆戻りしない。`extraIngredients` と `refreshLabel` の表示分岐は維持（初期表示で pin された具材の見え方は変わらない）。Jest 11 suites / 76 tests 緑、`npx tsc --noEmit` 緑。プランは [docs/plans/archive/dish-item-demoted-on-ai-refresh.md](docs/plans/archive/dish-item-demoted-on-ai-refresh.md) に移動
- 2026-04-26 ローカルサーバ接続時に「AI レシピ検索でタイムアウト」が発生する問題の修正。`mobile/src/api/client.ts` の axios 既定 timeout は 30 秒だったが、`gemini-3-flash-preview`（thinking 系 preview）は tail latency が伸びると単発で 14 秒前後 + WSL2 ↔ 実機経路と Wi-Fi のジッタで 30 秒を超えるケースが発生していた。`mobile/src/api/ai.ts` の `suggestAi` だけ `client.post` の第 3 引数に `{ timeout: 60000 }` を渡して 60 秒に延長（操作系 API は UX のため 30 秒のまま）、`mobile/__tests__/api/ai.test.ts` の通常呼び出しと extraIngredients 呼び出しの assert を 3 引数形式に書き換え、Jest 11 suites / 76 tests 緑、実機の Expo Go でローカルサーバ接続時の AI レシピ検索がタイムアウトしないことを確認。プランは [docs/plans/archive/ai-suggest-timeout.md](docs/plans/archive/ai-suggest-timeout.md) に移動。サーバ側 `askGemini` のタイムアウト追加は別タスク（必要になれば）
- 2026-04-26 ログインで未ログインのローカルデータが race condition で消える問題の修正（5 Phase 構成）。`auth-store.verify()` から `isAuthenticated=true` のセットを外して token 保存と `pendingEmail` 保持のみに、`finishLogin()` を新設して `getMe` → `setMode('server')` → `loadAll`/`loadSavedRecipes`/`loadQuota` → `isAuthenticated=true` の正規ルートに集約、`cancelLogin()` はローカルデータに触れず token 削除と auth state リセットだけにする。`_layout.tsx` の effect は `useRef` で起動時 1 回限定にし、ログイン／ログアウト操作では再発火しない（mode 切替・データロードは auth-store 側に一元化）。`migration.ts` から `switchStoresToServer` を削除し `runLoginMigration` は `migrated`/`discarded`/`cancelled` の結果文字列を返すだけにして mode 切替判断を呼び出し側に寄せる。Phase 5 として、ログアウトでも画面から食材・料理・レシピが消えないよう `resetLocalStores` から `clearLocalData` 呼び出しを外し、`setState({ mode: 'local' })` で mode だけ書き換える形に変更（持っているのはサーバ ID だが local モードの操作は id 一致で in-memory に書くので動作する。再ログインで「移す」を選ぶとサーバに重複登録される可能性は既知トレードオフ）。`mobile/__tests__/stores/auth-store.test.ts` を新フローに書き換え、`mobile/__tests__/components/auth-modal-flow.test.ts` を新設して `handleVerify` の migrated/discarded/cancelled/空ローカル の 4 経路を結合テスト、Jest 11 suites / 76 tests 緑、Vitest 20 files / 227 tests 緑、Expo Go 実機で 1〜6 の手動シナリオ + ログアウト後にローカルデータが残ることを確認。プランは [docs/plans/archive/login-data-loss.md](docs/plans/archive/login-data-loss.md) に移動
- 2026-04-26 アプリ起動直後にハンバーガーメニューの AI 残量を表示。サーバ側に副作用なしの `GET /api/ai/quota` を新設し（`server/src/services/ai-quota-service.ts` に `getJstDate` / `getJstResetAtIso` / `hashDeviceId` / `getAiQuotaSnapshot` を集約、`rate-limit-ai` ミドルウェアはサービス利用に書き換え、`/api/ai` マウントから `rateLimitAi` を外して `/suggest` 単独に再配置）、モバイル側は `mobile/src/api/ai.ts` に `getAiQuota` / `mobile/src/stores/ai-store.ts` に `loadQuota`（fail-soft）を追加し、`mobile/app/_layout.tsx` の auth-effect 末尾で `useAiStore.getState().loadQuota()` を呼んで起動時とログイン／ログアウト切替時に再取得、`auth-store.logout` の `resetLocalStores` で `useAiStore.reset()` も呼んでユーザー枠の数字が一瞬残るのを防止。Vitest 21 ケース・Jest 11 ケースを追加して全テスト緑、Expo Go 実機でログイン／未ログイン／ログイン→ログアウト・AI 実行時の残量更新を確認。プランは [docs/plans/archive/ai-quota-on-startup.md](docs/plans/archive/ai-quota-on-startup.md) に移動
- 2026-04-26 `mobile-build-local.sh` の LAN IP 検出を `hostname -I` の先頭から `ip route get 1.1.1.1` の `src` に切り替え（WSL2 で eth0 の NAT IP `10.5.0.x` が先頭に来るとスマホから到達できずログインが失敗する問題への対応、macOS は `ipconfig getifaddr en0/en1`、最後のフォールバックとして `hostname -I` を残す）
- 2026-04-26 ローカルサーバ接続時の表示追加: `mobile/src/config/api-endpoint.ts` に Phase 1 で追加した `isLocalServer` / `localServerLabel` を `mobile/app/(tabs)/_layout.tsx` で消費し、ハンバーガー `☰` の右上に赤丸バッジ、メニュー最下行に `🔧 ローカル: {host}:{port}` を表示（本番接続時は両方非表示で既存メニューと同じ見た目）。動作確認中に `mobile/.env` の古い `EXPO_PUBLIC_API_URL=http://10.0.1.137:3000` が prod 起動時にも効いて誤判定する問題が判明したため、`mobile/.env` を削除（build スクリプトがシェル経由で env を渡すので不要）し、`mobile-build-local.sh` / `mobile-build-prod.sh` に「前回起動時の URL を `mobile/.expo/.last-api-url` に記録し、変わったときだけ `--clear` を付与」するロジックを追加（`EXPO_PUBLIC_*` は babel が文字列として inline するため Metro キャッシュで古い値が残るのを防ぐ）。プランは [docs/plans/archive/mobile-dev-server-indicator.md](docs/plans/archive/mobile-dev-server-indicator.md) に移動
- 2026-04-26 mobile-build スクリプトの tunnel 切り替え対応。`mobile-build-local.sh` / `mobile-build-prod.sh` の `--tunnel` ハードコードを外し、`"$@"` 透過渡しでユーザが必要なときだけ `expo start` に `--tunnel` を渡せる形に。`./mobile-build-local.sh --tunnel` で ngrok URL が出る動作を確認。README とプランに使い方を反映
- 2026-04-26 expo クライアントアプリビルドスクリプトを整備。`mobile-build-local.sh`（LAN サーバ接続の Expo Go 起動）/`mobile-build-prod.sh`（本番サーバ接続の Expo Go 起動）/`mobile-submit-testflight.sh`（`eas build --auto-submit` で production ビルド + TestFlight 提出）の 3 本をリポジトリルートに追加。`mobile/eas.json` から preview/development の `EXPO_PUBLIC_API_URL` プレースホルダを除去（フォールバック `https://basket.chobi.me` に任せる）。`--tunnel` などの追加引数は `"$@"` 透過渡しで `expo start` に流せる。README に「モバイルアプリ」節を追加して使い方を記載
- 2026-04-26 CLAUDE.md に「作業着手ルール」セクションを追記。作業開始時に `docs/plans/<task-name>.md` を作成すること、TODO.md に該当項目が無ければ追加してプランファイルへのリンクを記載すること、複数 Phase / Step がある場合は子タスクとして列挙すること、完了時は親タスクを DONE.md に移しプランファイルを `docs/plans/archive/` に移動すること、を明文化
- 2026-04-26 表示テキスト変更: `mobile/src/components/dishes/IngredientsScreen.tsx:302` の「追加素材（買い物リストから）」を「追加具材（買い物リストから）」に変更
- 2026-04-24 AI 取得フロー統一 Phase 5: クライアント側テスト更新（`mobile/__tests__/stores/shopping-store.test.ts` の `shopping` モックから `getItemSuggestions: jest.fn()` を、`dishesApi` モックから `getDishSuggestions: jest.fn()` を削除、`suggestAi` 呼び出しの assert を `('カレー', undefined, 'both')` から `('カレー', undefined)` に変更、`mode='ingredients'` で `recipes_json` を温存するテストと `mode='recipes'` で auto-save を検証するテストの 2 ブロックを丸ごと削除。`mobile/__tests__/utils/migration.test.ts` の `shoppingApi` / `dishesApi` モックから `getItemSuggestions` / `getDishSuggestions` を削除。`mobile/__tests__/api/ai.test.ts` の通常呼び出しと `extraIngredients` 呼び出しの assert から `mode: 'both'` フィールドを除外、`mode='ingredients'` / `mode='recipes'` を渡すテスト 2 ブロックを削除。`grep -rn "SuggestAiMode\|getDishSuggestions\|getItemSuggestions\|mode: 'both'\|mode: 'ingredients'\|mode: 'recipes'" mobile/` でヒット 0 件を確認、`cd mobile && npm test` で 9 suites / 56 tests 緑）
- 2026-04-24 AI 取得フロー統一 Phase 4: `IngredientsScreen` の表示ロジック整理（`mobile/src/components/dishes/IngredientsScreen.tsx` の `import` から `SuggestAiMode` を外す、`fetchSuggestions` の第 2 引数 `mode` と `if (mode !== 'ingredients')` 分岐を削除して常に `setRecipes` / `setRecipeStates` を更新する 1 ルートに統合、`AiQuotaError` 時の retry も `fetchSuggestions(extras)` に変更、`handleFetchIngredients` と `fetchIngredientsLabel` を削除、`refreshLabel` の 3 分岐（「この素材でレシピを再検索」/「レシピを AI で取得」/「レシピを再検索」）を 2 文言（`extraIngredients > 0` → 「この素材でレシピをAI検索」/ それ以外 → 「レシピをAI検索」）に統合、empty 状態の専用ボタン `<TouchableOpacity onPress={handleFetchIngredients}>` と説明文「この料理の具材はまだ取得していません。」を削除し、`ingredients.length === 0` でも `extraIngredients.length > 0` のときは既存「追加素材」セクション（`+ name` 形式の破線チップ）をそのまま表示・extras 0 のときは `handleRefresh` のボタンのみ表示する 1 系統に統合、ローディングテキストを `'具材を検索中...'` から `'AI で検索中...'` に変更、未使用になった `emptySection` / `emptyText` スタイルを削除、`recipes` セクションのフッターボタン文言も新 `refreshLabel` を流用、AI 呼び出しの onPress は `handleRefresh`（extras なし）と `handleSearchWithExtras`（extras あり）の 2 種類だけになった、`cd mobile && npx tsc --noEmit` で `IngredientsScreen.tsx` 関連の型エラーが解消（`__tests__/` 側はまだ `mode='both'` を渡しており Phase 5 で解消予定））
- 2026-04-24 AI 取得フロー統一 Phase 3: クライアント側 API / ストアから `mode` 系統を撤去（`mobile/src/api/ai.ts` から `SuggestAiMode` 型を削除し `suggestAi` の `mode` 引数とリクエスト body の `mode` フィールドを削除、`mobile/src/stores/shopping-store.ts` の `import` から `SuggestAiMode` を外し `suggestIngredients` のシグネチャと本体から `mode` 引数・`ingredientsOnly` 分岐を削除して常に `ingredients` と `recipes` の両方を Zustand と DB キャッシュ（`updateDishAiCache`）に書き戻す 1 ルートに統合、`mobile/src/api/dishes.ts` から `getDishSuggestions` 関数を削除（`updateDishAiCache` は維持）、`mobile/src/api/shopping.ts` から `getItemSuggestions` 関数を削除。`IngredientsScreen` と `__tests__` 側はまだ `SuggestAiMode` / `mode` 引数を参照している状態で、Phase 4 / Phase 5 で解消予定）
- 2026-04-24 AI 取得フロー統一 Phase 2: サーバ側テスト更新（`server/tests/integration/ai.test.ts` の `describe('mode parameter', ...)` ブロックと `INGREDIENTS_ONLY_RESPONSE` を削除、代わりに「旧 `mode` フィールドを送っても無視される」1 ケースを残す。`server/tests/unit/dish-ai.test.ts` から `buildIngredientsOnlyPrompt` の import と describe を削除し、`parseDishInfo` の「ingredients-only JSON」テストを削除（markdown code fence のテストはレシピ形式 JSON に書き換え）。`server/tests/unit/dish-service.test.ts` に「同名の料理を削除→再登録すると `ingredients_json` / `recipes_json` が NULL のまま新規作成される」回帰テストを追加。`server/tests/integration/dishes.test.ts` から `GET /api/dishes/suggestions` describe ブロックを削除。`server/tests/integration/shopping.test.ts` から `GET /api/shopping/suggestions` describe ブロックと `recordPurchase` import を削除。`server/tests/unit/shopping-service.test.ts` から `getSuggestions` describe ブロックと該当 import を削除（`recordPurchase` は updateItem テストで継続使用のため import 削除）。`cd server && npm test` で 17 files / 192 tests 緑）
- 2026-04-24 AI 取得フロー統一 Phase 1: サーバ側 service / route から `mode` 分岐を撤去（`server/src/routes/ai.ts` の `SuggestMode` 型・`VALID_MODES` 定数・`requestedMode` バリデーション・プロンプト分岐を削除し常に `buildDishInfoPrompt` を直呼び）、`server/src/services/dish-ai.ts` から `buildIngredientsOnlyPrompt` 関数と `parseDishInfo` の「具材のみ形式」分岐を削除、`server/src/services/dish-service.ts` の `createDish` から「同名過去料理の AI キャッシュ継承」処理を削除し（料理 ID で識別するため同名再登録は別物として扱う）併せて `DishSuggestion` interface と `getDishSuggestions` 関数も削除、`server/src/routes/dishes.ts` から `GET /suggestions` ルートを削除、`server/src/services/shopping-service.ts` から `PurchaseSuggestion` interface と `getSuggestions` 関数を削除（`recordPurchase` と `purchase_history` テーブルへの INSERT は admin 画面のため残す）、`server/src/routes/shopping.ts` から `GET /suggestions` ルートを削除）
- 2026-04-24 管理画面にサーバのデプロイ日時を表示 Phase 2: `/admin/` システム情報ページの「サーバー」セクションに「デプロイ日時」行を稼働時間の直下に追加（`web/admin/app.js` の `renderSystem()` で `s.deployedAt` が truthy なら `new Date(s.deployedAt).toLocaleString('ja-JP')` を `escapeHtml` 経由で表示、falsy なら muted 色 `#94a3b8` / font-weight 400 で「未設定」を表示、`cd server && npx tsc --noEmit` 緑）
- 2026-04-24 管理画面にサーバのデプロイ日時を表示 Phase 1: サーバ側実装（`server/.env.example` に `DEPLOYED_AT` のコメント付きテンプレート行を追加、`server/src/services/admin-service.ts` の `getSystemInfo()` 戻り値に `deployedAt: string | null` を追加しヘルパ `getDeployedAt()` で `process.env.DEPLOYED_AT` を読み空文字/undefined/`Date.parse` が NaN のときは `null` を返し、バリデーションを通った文字列はそのまま返す（再整形しない）、`server/tests/unit/admin-service.test.ts` を新設し `setupTestDatabase()` を取り回して `beforeEach`/`afterEach` で `DEPLOYED_AT` をクリーンに管理しつつ「未設定→null」「有効な ISO 8601→文字列をそのまま返す」「`'not-a-date'`→null」の 3 ケースをカバー、`cd server && npm test`（14 files / 136 tests）が緑）
- 2026-04-24 本番サーバで `/api/ai/suggest` が未ログイン時に `DEVICE_ID_SECRET が設定されていません` で 500 落ちしていた件を修正（原因は本番コンテナへ `server/.env` の envs が渡っておらず `DEVICE_ID_SECRET` 未設定のため `rate-limit-ai.ts` の `hashDeviceId` が throw していた。デプロイシステム側で env_file から注入するように直し、リポジトリからは `docker-compose.yml` を削除してデプロイ定義の単一ソース化）
- 2026-04-21 管理画面で docs/plans, docs/specs のマークダウンドキュメントを閲覧可能に（API追加 + 一覧/閲覧画面）
- 2026-04-21 dev-admin Step1: dev-admin/ ディレクトリ作成、package.json / tsconfig.json 作成
- 2026-04-21 dev-admin Step2: Express サーバ実装（/api/docs, /api/docs/:category/:file, /api/design/:file、ポート 3010、127.0.0.1 バインド）
- 2026-04-21 dev-admin Step3: dev-admin/src/web/ に index.html / style.css / app.js を作成（サイドバー + markdown 表示 + design iframe プレビュー）
- 2026-04-21 dev-admin Step4: ルートに dev-admin.sh 起動スクリプトを追加
- 2026-04-21 dev-admin Step5: 動作確認（/api/docs で plans / specs / design 一覧、/api/docs/:category/:file の markdown→HTML 変換、/api/design/:file の HTML 配信、静的 Web UI、path traversal / 拡張子チェックが正しく動作）
- 2026-04-21 dev-admin Step6: 既存 admin からドキュメント関連機能を削除（server/src/routes/admin.ts の /docs-files エンドポイント・関連コード・未使用 import、web/admin/app.js の renderDocs / renderDocFile / DOC_CATEGORY_LABELS / doc-file ルーター、web/admin/index.html のサイドバー「企画ドキュメント」項目）
- 2026-04-21 dev-admin Step7: CLAUDE.md に dev-admin の起動方法を追記（./dev-admin.sh、ポート 3010、DEV_ADMIN_PORT での上書き）
- 2026-04-21 dev-admin レイアウト変更 Step1: サーバ `/api/docs` をツリー構造レスポンス（`{files, dirs}`）に変更、`listTree` 再帰実装、md ファイル取得をカテゴリ配下のサブディレクトリまで再帰探索に対応
- 2026-04-21 dev-admin レイアウト変更 Step2: `dev-admin/src/web/index.html` にトップタブ DOM（Plans / Specs）を追加
- 2026-04-21 dev-admin レイアウト変更 Step3: `app.js` にタブ切替・ツリー描画（再帰）・折りたたみ・localStorage 連携・旧 `#design/` ハッシュの `#specs/design/` リダイレクトを実装
- 2026-04-21 dev-admin レイアウト変更 Step4: `style.css` にトップタブ・ツリーのインデント・トグルアイコンのスタイルを追加
- 2026-04-21 dev-admin レイアウト変更 Step5: 動作確認（タブ切替でサイドバー差し替え / `specs/design/` の折りたたみ / タブ・折りたたみ状態の localStorage 復元 / 既存 `#plans/xxx.md` 等のハッシュ互換 / 旧 `#design/xxx.html` → `#specs/design/xxx.html` リダイレクト / サーバ側 API レスポンス・エラーハンドリング）
- 2026-04-21 dev-admin 画面全体を上下2段レイアウトに変更（上段: `dev-admin` ブランド + Plans/Specs タブ + ページタイトル + パス、下段: サイドバーのファイルツリー + コンテンツ。トップバーをフル幅に昇格、サイドバーは固定配置から flex レイアウト内のスクロール領域に変更）
- 2026-04-21 dev-admin を GitHub 風ライトテーマに再配色（サイドバーを角丸ピル化、アクティブタブをオレンジ下線に、インデントを margin ベースに変更）
- 2026-04-21 plans の md ドキュメントに「アーカイブする」ボタンを追加（`docs/plans/<file>.md` → `docs/plans/archive/<file>.md` に移動する `POST /api/docs/plans/:file/archive` エンドポイントを追加、`docs/plans/archive/` ディレクトリを用意）
- 2026-04-21 ログインなしで使えるようにする: 変更プラン作成（`docs/plans/no-login-mode.md` に設計検討を、`docs/plans/no-login-mode-impl.md` に確定実装プラン（Phase 1-5 分割・API 最終形・リスク整理）を追加）
- 2026-04-22 dev-admin TODO エディタ Phase 1: サーバ側 API（`EDITABLE_FILES` ホワイトリスト追加、`GET /api/files/:name` / `GET /api/files/:name/render` / `PUT /api/files/:name` 実装、mtime 楽観ロックで 409、tmp→rename のアトミック書き込み、curl で 200/409/400/404 動作確認）
- 2026-04-22 dev-admin TODO エディタ Phase 2: SSE 変更通知（`GET /api/files/watch` を `text/event-stream` で実装、`fs.watch` + `stat` による mtime 比較重複排除、2 秒ポーリングのフォールバック・30 秒 keep-alive ping・切断時 watcher 解除、`/api/files/:name` より前にマウントしてルート衝突を回避。curl -N と `echo >>` で TODO.md / DONE.md の変更通知を確認）
- 2026-04-22 dev-admin TODO エディタ Phase 3: クライアント UI 閲覧（topbar に「TODO」タブを追加・`CATEGORIES` 拡張、`renderTodoView()` で左 TODO/DONE リンク・右プレビューのレイアウトを実装、`GET /api/files/:name/render` の HTML を注入。TODO タブで TODO.md / DONE.md が整形表示されることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 4: クライアント UI 編集（プレビュー/編集のサブタブ + textarea + 保存/破棄ボタン、`PUT /api/files/:name` + `baseMtime` での保存と 409 時のダイアログ[リロード / 維持 / 強制上書き]、`isDirty` 管理と `beforeunload` 離脱警告、保存成功トーストを実装。編集・保存で実ファイルが更新され、外部変更時は 409 ダイアログが出ることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 5: リアルタイム反映と競合表示（`EventSource('/api/files/watch')` を init で接続、`onopen/onerror` で topbar の切断インジケータを制御、`change` 受信時にモード別分岐: プレビューは自動リフレッシュ + 「外部で更新されました」バッジ、編集 clean は textarea と mtime 差し替え + 情報バー通知、編集 dirty は黄色警告バー + `[差分を見る]`（手元 vs 外部の 2 ペイン）/`[外部版を読み込む]`/`[このまま編集を続ける]` の 3 アクション。多層サインとして競合中はサイドバーに赤●バッジ・タブタイトルに `(!) ` を prepend。保存直後の自己書き込みを `selfWrittenMtimes` / `saveInFlight` で除外。curl -N で外部編集時の SSE 通知を確認）
- 2026-04-22 dev-admin TODO エディタ Phase 5b: 手動再取得（TODO ビューのツールバーに `↻ 再取得` ボタンを常設、tooltip に最終取得時刻を表示・未反映の外部更新があるときはボタンを色強調、プレビュー / clean 編集時はトースト付きで再取得・dirty 編集時は confirm で破棄確認、`R` 単独キーショートカット（textarea/input 非フォーカス時のみ、Cmd/Ctrl+R はブラウザリロードに譲る）を実装。SSE 切断状態でも再取得ボタン経由で最新内容を取得できることを確認）
- 2026-04-22 dev-admin TODO エディタ Phase 6: 仕上げ（style.css に TODO ビュー用スタイル [todo-editor 等幅 textarea、トースト、モーダル、差分 2 ペイン、SSE 切断インジケータ、外部更新バッジ、情報バー、黄色競合バー、サイドバー赤●バッジ、再取得ボタン emphasized] が網羅されていることを確認、CLAUDE.md の dev-admin セクションに TODO 編集・SSE リアルタイム反映・楽観ロック・再取得 UI を追記、回帰確認として Plans/Specs の `/api/docs` ツリー・`/api/docs/plans/:file.md` レンダリング・`/api/docs/specs/:file.md` レンダリング・archive ディレクトリの列挙・TODO.md の GET / render / watch エンドポイントが全て 200 で動作することを確認）
- 2026-04-22 テスト導入 Phase 1: サーバ側テスト基盤（vitest / @vitest/coverage-v8 / supertest / @types/supertest を server/ に導入、`server/vitest.config.ts`（`environment: 'node'`・`pool: 'forks'`・`setupFiles`）作成、`package.json` に `test` / `test:watch` / `test:coverage` 追加、`src/app.ts` に `createApp()` を切り出して `src/index.ts` は `listen` のみに薄型化、`tests/setup.ts` で `JWT_SECRET` / 外部 API キー / `DB_PATH=/tmp/cb-test-<pid>.db` を強制・本体 `shopping.db` 防衛ガード、`tests/helpers/db.ts`（`initDatabase` + `truncateAllTables` + `closeDatabase`）と `tests/helpers/auth.ts`（`createTestUser` / `createAuthHeader`）・`tests/helpers/app.ts` を整備、`database.ts` に `closeDatabase()` をテスト用途で追加、スモークテスト `tests/integration/health.test.ts` を追加して `npm test` が緑・`npm run build` も通ることを確認、本体 `shopping.db` の mtime/size が変化しないことを確認）
- 2026-04-22 テスト導入 Phase 3: サーバ route 層統合テスト（`tests/integration/` 配下に 4 本を追加、計 102 tests 緑。`auth.test.ts` = `vi.mock('resend')` で Resend クラスをモック化し `/api/auth/login` → `/api/auth/verify-code` の 2 ステップ（Resend の `to`/`text` からコードを回収 → verify で JWT 取得 → `split('.')` が 3 要素）、email の trim/lowercase、login の 400（missing / no @）、verify の 400/401（missing / unknown / wrong code / expired / 再利用拒否）、`/api/auth/me`（Bearer 無し 401・無効トークン 401・正常時は userId+email 返却）、`/api/auth/google-client-id` の結線確認。`shopping.test.ts` = 認証欠落系（no header / malformed / bad Bearer = 401）、POST の 201 と 400（空/空白）と trim、GET のユーザースコープ、PUT の checked 反映/404（存在しない・他ユーザー）、DELETE の 200/404、`DELETE /api/shopping/checked` の bulk 削除件数、`PUT /api/shopping/reorder` の並び替えと 400（非配列）、`GET /api/shopping/suggestions` の除外挙動と `?q=` 前方一致。`dishes.test.ts` = 認証 401、CRUD（POST 201・400・GET スコープ・PUT rename/400/404・DELETE が soft-delete で GET から消える/404）、食材リンク（POST 200 後に items に反映 / 400 itemId 欠落 / DELETE unlink / 404 未リンク）、`PUT /api/dishes/reorder` と `PUT /api/dishes/:id/items/reorder` の並び替え + 400、`GET /api/dishes/suggestions` が削除済み（active=0）料理名を返す。Gemini 呼び出しを含む `/:id/suggest-ingredients` は no-login 移行で廃止予定のため対象外（plan 通り）。`saved-recipes.test.ts` = 認証 401、POST 201（steps/ingredients が JSON 文字列で保存）・400（dishName/title 欠落）、GET 一覧のユーザースコープ、GET 個別の 200/404/他ユーザー 404、PUT `/like` のトグル挙動（1↔0・like_count）と 404、複数ユーザーの like 集計（3 人で like_count=3）、GET `/shared` が like>0 のみ返し `liked` が viewer 視点を反映（Alice 視点 0・Bob 視点 1）、未 like 時は空配列、DELETE の 200/404。カバレッジ: `shopping.ts` 97.67%・`saved-recipes.ts` 87.23%・`auth.ts` 70%（Google OAuth 経路は検証不能で意図的に除外）・`dishes.ts` 56.61%（未カバー行の大半は plan で除外した `/suggest-ingredients`）。`src/app.ts` 90.62% / `src/database.ts` 100%）
- 2026-04-22 テスト導入 Phase 4: モバイル側テスト基盤（`mobile/` に jest / jest-expo@~54 / @types/jest / ts-jest を devDependency として導入、`babel.config.js`（`babel-preset-expo`）を新設、`jest.config.js` で `jest-expo/jest-preset` を展開し `testMatch: __tests__/**/*.test.{ts,tsx}` と `setupFiles` を結合、`jest.setup.ts` で `expo-secure-store` をインメモリ Map モック化（`__reset` ヘルパ付き）、`package.json` に `test: jest` を追加。テストは 4 ファイル / 16 tests が緑: `__tests__/smoke.test.ts`（bootstrapping 確認）、`__tests__/stores/shopping-store.test.ts`（`api/shopping` と `api/dishes` を `jest.mock`、`addItem` が createItem→loadAll 経由で state を反映、`toggleCheck` が `items` と `dishes[].items` の両方を楽観更新し無関係な item には触れない、`reorderItems` が orderedIds を api にそのまま渡す、`deleteItem` が state と dish 配下の入れ子 items から対象 id を除去しつつ loadAll を呼ばない）、`__tests__/stores/auth-store.test.ts`（`login` は requestLogin 経由だけで isAuthenticated を変えない、`verify` が JWT を SecureStore に保存し state を authenticated に更新、`logout` が token 削除＋state リセット、`checkAuth` の 3 パス＝トークン無し・getMe 成功・getMe 失敗で `isLoading` が確実に false 化）、`__tests__/api/client.test.ts`（axios `interceptors.request.handlers` を直接呼び出し、token 保存時に `Authorization: Bearer <token>` が付与／未保存時は未設定のまま、`interceptors.response.handlers.rejected` を呼び出し 401 時に `SecureStore.deleteItemAsync` が走り 500 など他ステータスでは token が残る）。`cd mobile && npm test` が 1s 台で緑）
- 2026-04-22 テスト導入 Phase 2: サーバ service 層ユニットテスト（`tests/unit/` 配下に 4 本を追加、計 41 tests 緑。`shopping-service.test.ts` = createItem/getAllItems の userId スコープ・position シフト・category 既定値、updateItem で checked 0→1 時のみ purchase_history 記録（重複なし）、他ユーザー item の更新拒否、deleteCheckedItems の件数返却、getSuggestions が未チェック shopping_items を除外しチェック済みは候補に残す・前方一致・case-insensitive・ユーザー横断しない。`dish-service.test.ts` = createDish→linkItemToDish→unlinkItemFromDish→deleteDish のライフサイクル（deleteDish は active=0 のソフト削除、リンクしていた shopping_item は dish_id=NULL で残存）、他ユーザー item をリンク不可、reorderDishes / reorderDishItems が position を 0 起点で振り直し他ユーザーに影響しない。`saved-recipe-service.test.ts` = create / getAllSavedRecipes / getSavedRecipe の owner スコープ、deleteSavedRecipe の権限制御、toggleLike の trivial なトグルと複数ユーザーの like_count 集計、getSharedRecipes が like>0 のみ返し liked を viewer 視点で反映し like_count desc で並ぶ。`auth-service.test.ts` = `vi.mock('resend')` で Resend をクラスモック化しネットワーク遮断、findOrCreateUser の idempotency、createMagicLinkToken→verifyOtpCode→generateJwt の 3 ステップ（6 桁コード / 64 hex トークン / JWT payload 検証）、使用済みトークンの再利用拒否・新トークン発行時に旧トークン無効化・期限切れトークン検出&mark used、last_login_at 更新、cleanupExpiredTokens が使用済み＋期限切れを削除し有効な未使用のみ残す、sendOtpEmail が Resend.emails.send を 1 回だけ呼び宛先とコードを載せる。`npm test` で 5 files / 41 tests が緑）
- 2026-04-22 テスト導入 Phase 5: CI 連携（`.github/workflows/test.yml` を新規作成。`push` を `main` に、`pull_request` を全ブランチでトリガ。`strategy.matrix.project: [server, mobile]` で並列 2 ジョブ・`fail-fast: false`・`defaults.run.working-directory: ${{ matrix.project }}`。各ジョブは `actions/checkout@v4` → `actions/setup-node@v4`（Node 20・`cache: npm`・`cache-dependency-path: ${{ matrix.project }}/package-lock.json`）→ `npm ci` → `npm test`。better-sqlite3 の native build は Node 20 での `npm ci` でカバー。ローカルで `cd server && npm test`（9 files / 102 tests）と `cd mobile && npm test`（4 files / 16 tests）が緑を再確認。README 先頭に `test` ワークフローのバッジを追加し、末尾に「## テスト」セクション（`server` / `mobile` のコマンド・CI 動作・`docs/plans/testing.md` への誘導）を追記）
- 2026-04-22 ログインなしで使えるようにする Phase 1: サーバ側基盤（`server/src/database.ts` に `ai_quota(key,date,count)` テーブル追加、`server/src/middleware/auth.ts` に `optionalAuth` 追加、`server/src/middleware/rate-limit-ai.ts` を新規作成し `req.userId` 有無で `user:<id>`/`device:<sha256(raw+DEVICE_ID_SECRET)>` に分岐して JST 日次 UPSERT・上限超過で `429 ai_quota_exceeded`・成功時に `X-AI-Remaining` ヘッダ付与、`server/src/routes/ai.ts` 新設 `POST /api/ai/suggest` + プロンプト/パース処理を `server/src/services/dish-ai.ts` に分離、`server/src/routes/saved-recipes.ts` に `POST /bulk` 追加と `savedRecipesSharedRouter`（`optionalAuth` で `liked=0` 分岐）を切り出し、`server/src/services/saved-recipe-service.ts` の `getSharedRecipes` を userId optional 化・`createSavedRecipesBulk` 追加、`server/src/routes/dishes.ts` から `/:id/suggest-ingredients` ハンドラを削除し `PUT /:id/ai-cache` を新設、`server/src/routes/migrate.ts` 新設（body: items/dishes/savedRecipes、トランザクションで一括 INSERT、localId→サーバ ID の 3 マップを返却）、`server/src/app.ts` で `/api/ai` を `optionalAuth`+`rateLimitAi` で、`/api/saved-recipes/shared` を認証ガードなしで先行マウントし `/api/migrate` は `requireAuth`、廃止した `server/src/routes/claude.ts`・`recipes.ts`・`services/claude-service.ts` を削除、`.env`/`.env.example` に `DEVICE_ID_SECRET`/`AI_LIMIT_USER=20`/`AI_LIMIT_GUEST=3` を追加、`tests/setup.ts` に同環境変数を固定、`tests/integration/ai.test.ts`（Gemini を `vi.mock` でスタブ、guest 200＋`X-AI-Remaining=2`・X-Device-Id 欠落 400・3 回連続後 429＋`resetAt`・別デバイスで独立・ログイン時 20 回・dishName 欠落/空白 400 の 6 ケース）と `tests/integration/migrate.test.ts`（401・import→id マップ＋サーバ反映＋ dish/item リンク復元・空 body・ユーザー分離）を新設、`tests/integration/saved-recipes.test.ts` に `POST /bulk`（201＋400×2）と `GET /shared` 未ログインパスを追加、`tests/integration/dishes.test.ts` に `PUT /:id/ai-cache` の 200/400/404 を追加、`docs/specs/shopping-list.md` から `/api/recipes/recommend`・`/:id/suggest-ingredients` 節を削除し `/api/ai/suggest`＋`/ai-cache` を記載・廃止 API 注記を追加、`npm run build`・`npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 1 動作確認: curl でサーバ単体動作確認（未ログイン `POST /api/ai/suggest` のハッピーパス＋`X-AI-Remaining` 減算・3 回到達で 429＋`resetAt`・`X-Device-Id` 欠落で 400・`dishName` 空で 400、ログイン時 `X-AI-Remaining: 19` 起点、`GET /api/saved-recipes/shared` が未ログインでも 200 かつ `liked` は常に 0、`POST /api/migrate` が 201 で `dishIdMap`/`itemIdMap`/`savedRecipeIdMap` を返し `savedRecipes.source_dish_id` が解決済み ID を指す・未認証で 401、廃止ルート `/api/claude`・`/api/recipes/recommend`・`POST /api/dishes/:id/suggest-ingredients` が 404、`npm test` 緑）
- 2026-04-22 ログインなしで使えるようにする Phase 2 動作確認: 新規起動（未ログイン）でタブがそのまま開けること、☰メニューの「ログイン」から AuthModal が出て メール → コードの 2 ステップで認証成功し、成功時にモーダルが自動で閉じて☰メニューが `<email>` + 「ログアウト」表示に切り替わることを実機確認
- 2026-04-22 ログインなしで使えるようにする Phase 2: モバイル認証 UI 差し替え（`mobile/` に `expo-application` / `expo-crypto` を `npx expo install` で導入、`mobile/src/utils/device-id.ts` 新設（iOS は `Application.getIosIdForVendorAsync()`、Android は `Application.getAndroidId()`、どちらも null のときは `Crypto.randomUUID()` を `expo-secure-store` にキャッシュ、生 ID をそのまま返しハッシュ化はサーバに委譲、モジュールスコープにメモキャッシュ＋テスト用 `__resetDeviceIdCacheForTests`）、`mobile/src/api/client.ts` の request interceptor で JWT が無いときのみ `X-Device-Id` を付与（失敗は best-effort で無視）、`mobile/src/stores/auth-store.ts` を拡張して `authModalVisible`/`authModalReason`/`authModalOnSuccess` を追加、`requestLogin({ reason, onSuccess })`/`closeAuthModal()` を新設、既存 `login(email)` はインポート名衝突を避けて `sendMagicCode(email)` にリネーム、`verify` は成功時に保存した `authModalOnSuccess` を呼びつつ modal を閉じる、`mobile/src/components/auth/AuthModal.tsx` 新設（メール→コードの 2 ステップ、`KeyboardAvoidingView` + transparent `Modal`、reason ラベル表示、ローディング中はボタンを `ActivityIndicator` 化、キャンセル/別アドレス リンク付き）、`mobile/app/_layout.tsx` から `segments`/`useRouter` ベースの未認証リダイレクトを撤去し `<Slot>` の兄弟として `<AuthModal>` を常設、`mobile/app/(auth)/` 配下（`_layout.tsx`/`login.tsx`/`verify-code.tsx`）を削除、`mobile/app/(tabs)/_layout.tsx` の☰メニューを `isAuthenticated` で分岐しゲスト時は「ログイン」、ログイン時は `<email>` + 「ログアウト」を表示、`mobile/jest.setup.ts` に `expo-application`/`expo-crypto` のモックを追加、`mobile/__tests__/stores/auth-store.test.ts` を `sendMagicCode` 対応に刷新し `requestLogin`/`closeAuthModal`/`verify` 時の onSuccess 発火・modal クリアまでカバー、`mobile/__tests__/api/client.test.ts` に JWT 無しで `X-Device-Id` が付与されるケースを追加、`mobile/__tests__/utils/device-id.test.ts` を新設（iOS IDFV・Android SSAID・null→UUID フォールバック・既存保存 UUID の再利用・モジュール内メモキャッシュの 5 ケース）、`npm test`（5 files / 24 tests）と `npx tsc --noEmit` が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 4: マイグレーション（`mobile/src/api/migrate.ts` 新設 `migrate({items, dishes, savedRecipes})` で `POST /api/migrate` を叩き `dishIdMap`/`itemIdMap`/`savedRecipeIdMap` を受け取る型付きラッパ、`mobile/src/utils/migration.ts` 新設 `runLoginMigration()` が `useShoppingStore` + `useRecipeStore` のローカルデータを集計し空なら黙って `setMode('server')`+`loadAll()`、非空なら `Alert.alert` で「移す / 破棄 / キャンセル」を問い合わせ、破棄時は二段目の警告ダイアログで確認後にローカル破棄、移行時は `items.id`→`localId`・`dish_id`→`dishLocalId`・`source_dish_id`→`sourceDishLocalId` にマッピングして JSON カラムをパースしながら送信、マイグレ例外時は Alert を出して `cancelled` を返す、`mobile/src/stores/auth-store.ts` の `verify` を「トークン保存＋isAuthenticated 更新」だけに薄型化しモーダル閉じ／onSuccess 発火の責務を `finishLogin`/`cancelLogin` アクションに分離、`logout` は `removeToken` に加えて `useShoppingStore.clearLocalData/setMode('local')` と `useRecipeStore.clearLocalData/setMode('local')` を呼んで前ユーザーのサーバデータを完全にパージ、`mobile/src/components/auth/AuthModal.tsx` の `handleVerify` を `verify` → `runLoginMigration()` → 結果に応じて `finishLogin`/`cancelLogin` を呼ぶフローに変更、`mobile/__tests__/stores/auth-store.test.ts` を `verify`/`finishLogin`/`cancelLogin`/`logout`（ローカルストアクリアを含む）に分割した 10 テストに再構成、`mobile/__tests__/utils/migration.test.ts` 新設で「空ローカル→プロンプト無し `migrated`」「移すで `migrate()` 呼び出しペイロード検証」「破棄＋二段目確認で `discarded`＋ローカルクリア」「キャンセルで `cancelled`＋ローカル保持」「migrate 失敗で `cancelled`＋ローカル保持」の 5 ケースを網羅、`cd mobile && npm test`（9 files / 56 tests）と `cd mobile && npx tsc --noEmit`、`cd server && npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 3: モバイルローカルストア化（`@react-native-async-storage/async-storage` を `npx expo install` で導入、`mobile/src/api/ai.ts` 新設で `suggestAi(dishName, extraIngredients?)` を実装し `x-ai-remaining` をレスポンスヘッダから抽出・429 `ai_quota_exceeded` を `AiQuotaError(resetAt)` クラスに変換、`mobile/src/api/dishes.ts` から `suggestIngredients()` を削除して `updateDishAiCache(dishId, ingredients, recipes)` に差し替え、`mobile/src/api/saved-recipes.ts` に `createSavedRecipesBulk(recipes)` を追加、`mobile/src/stores/ai-store.ts` 新設（`remaining`/`quotaExceeded`/`resetAt` state、`setRemaining`/`markQuotaExceeded`/`reset`）、`mobile/src/stores/shopping-store.ts` を zustand `persist` + AsyncStorage で包んで `mode: 'local' | 'server'`/`nextLocalId`（負の連番）を追加、全メソッドに local/server 分岐を入れ `addItem`/`addDish`/`linkItemToDish`/`toggleCheck`/`deleteItem`/`deleteCheckedItems`/`reorderItems`/`reorderDishes`/`reorderDishItems`/`updateItemName`/`updateDish`/`deleteDish` が local モードでは AsyncStorage 経由、server モードでは既存 API 経由で動くように、`suggestIngredients(dishId, extras?)` は `suggestAi` 呼出→`AiQuotaError` 時は `useAiStore.markQuotaExceeded`→成功時は `setRemaining`→dish の `ingredients_json`/`recipes_json` にキャッシュ→server モードでは `updateDishAiCache` も呼ぶ→`useRecipeStore.autoSaveRecipes` で自動保存→`recipeStates` を返却、`partialize` で local 時のみ items/dishes を永続化、`mobile/src/stores/recipe-store.ts` を `persist` + AsyncStorage で包んで `mode`/`nextLocalId` 追加、`autoSaveRecipes(dishName, recipes, sourceDishId)` が local では負 ID 採番・server では `POST /api/saved-recipes/bulk`、`toggleLike` は local モードで `useAuthStore.requestLogin` を呼んで AuthModal に誘導、`loadSharedRecipes` は未ログインでもサーバ経路で取得、`loadSavedRecipes` は local モードでは永続化された state を使うので no-op、`mobile/src/components/dishes/IngredientsScreen.tsx` を新 `suggestIngredients` フローに差し替え、前回キャッシュがあれば初期表示に使い無ければ AI 呼出、残回数ボタン文言「レシピを再検索（残り X 回）」、`AiQuotaError` 時は未ログイン→`requestLogin(reason, onSuccess=retry)`/ログイン済→「本日の上限に達しました」アラート、`mobile/app/(tabs)/shared.tsx` は未ログインでもレシピ閲覧可・いいね押下で `requestLogin`、`mobile/app/(tabs)/recipes.tsx` は local モードでは全 saved-recipes を表示（server モードのみ liked でフィルタ）・いいねは `requestLogin`、`mobile/app/_layout.tsx` に `isAuthenticated` と store mode を同期する effect を追加（認証成功で `server` に切替＋`loadAll`/`loadSavedRecipes`、ログアウトで `local` に戻す）、`mobile/jest.setup.ts` に AsyncStorage の in-memory モックを追加、`__tests__/stores/shopping-store.test.ts` を刷新し server モードの既存 4 ケース＋local モード 6 ケース（addItem で負 ID 採番・linkItemToDish でローカル紐付け・toggleCheck/deleteItem/deleteCheckedItems がローカル完結・suggestIngredients が ai/suggest→dish キャッシュ→local saved-recipes 自動保存＋`setRemaining`・`AiQuotaError` で `markQuotaExceeded`）＋setMode 2 ケースを網羅、`__tests__/stores/ai-store.test.ts` 新設（setRemaining の n<=0 で quotaExceeded 切替・null 時・markQuotaExceeded・reset）、`__tests__/stores/recipe-store.test.ts` 新設（server: loadSavedRecipes/toggleLike/autoSaveRecipes、local: loadSavedRecipes が no-op・autoSaveRecipes が負 ID 採番・toggleLike で requestLogin・deleteSavedRecipe がローカルのみ・setMode で state クリア）、`__tests__/api/ai.test.ts` 新設（ingredients/recipes/X-AI-Remaining 抽出・ヘッダ欠落で remaining=null・429 で AiQuotaError・他エラーは再 throw・extraIngredients 転送）、`npm test`（8 files / 49 tests）と `npx tsc --noEmit` が緑）
- 2026-04-23 実機アプリの接続先サーバ切り替え Phase 1: API URL を環境変数化（`mobile/src/config/api-endpoint.ts` を新設し `process.env.EXPO_PUBLIC_API_URL ?? 'https://basket.chobi.me'` を export、`mobile/src/api/client.ts` の `axios.create` の `baseURL` ハードコードを `API_BASE_URL` 参照に差し替え、`cd mobile && npm test`（9 files / 56 tests）が緑）
- 2026-04-23 実機アプリの接続先サーバ切り替え Phase 2: ビルドプロファイル別の env とローカル HTTP 許可（`mobile/eas.json` の `development` / `preview` プロファイルに `"env": { "EXPO_PUBLIC_API_URL": "http://192.168.x.x:3000" }` を追加・`production` は env 未指定のままフォールバックで本番 URL、`mobile/app.config.ts` を新設して `app.json` を `ConfigContext.config` 経由で継承し `process.env.EAS_BUILD_PROFILE === 'development' | 'preview'` のときだけ iOS `infoPlist.NSAppTransportSecurity.NSAllowsArbitraryLoads = true` と Android 側のクリアテキスト許可用プラグイン `["expo-build-properties", { android: { usesCleartextTraffic: true } }]` を plugins に追記、`mobile/` に `expo-build-properties@~1.0.10` を `npx expo install`、`mobile/.env.example` 新規作成（`EXPO_PUBLIC_API_URL` の書き方と未設定時の本番フォールバックを明記）、`mobile/.gitignore` に `.env` を追加、`npx expo config --type public` を profile 無し/`EAS_BUILD_PROFILE=preview` の両方で検証し production 側では ATS/cleartext 設定が混入しないこと・preview 側では両方が付与されることを確認、`cd mobile && npm test`（9 files / 56 tests）が緑）
- 2026-04-23 本番サーバログを外部から安全に閲覧 Phase 3: 管理 API 追加（`server/src/services/logs-service.ts` を新設し、`process.env.LOG_FILE_PATH` を起点に pino-roll の出力ファイル（`server.YYYY-MM-DD.N.log`）を最新 mtime で解決する `findActiveLogFile()`、`readRecentLogs(lines, filter)`（全行読み → JSON parse → `level >= threshold` と `JSON.stringify(entry).toLowerCase().includes(q)` でフィルタ → 末尾 N 件）、`tailLogFile(filter, onEntry)`（初期位置＝現在のファイルサイズ、1 秒ポーリング＋ディレクトリの `fs.watch` で `size > position` の差分を `createReadStream({start, end})` で読み JSON Lines に分解、日次ローテで active ファイル名が変わったら `position=0` でリセット、返り値で watcher + interval を破棄）を公開、`server/src/routes/admin.ts` に `GET /api/admin/logs?lines=200&level=info&q=` を追加（lines は 1..2000 にクランプ、デフォルト 200）、`GET /api/admin/logs/stream` を追加（`Content-Type: text/event-stream; charset=utf-8` / `Cache-Control: no-cache, no-transform` / `Connection: keep-alive` / `X-Accel-Buffering: no` を送出し `res.flushHeaders()` → 直近 50 件を `data: {JSON}\n\n` で吐いてから tail を開始、30 秒ごとに `: keep-alive\n\n` のハートビート、`req.on('close' | 'error')` で heartbeat の clearInterval と tail の unwatch を実行）、`server/tests/integration/admin.test.ts` を新設し未ログイン 401・非 admin 403・recent 取得（`msg` 配列の一致）・`?lines=` 末尾 N 件・`?level=warn` 閾値フィルタ・`?q=BAN` で case-insensitive 部分一致・非 JSON 行の黙殺・pino-roll の `server.2026-04-23.1.log` 解決・`LOG_FILE_PATH` 未設定で空配列、の 9 ケースと SSE smoke（`http.request` で `app.listen(0)` に接続 → `preexisting` を受信 → `fs.appendFileSync` で 1 行追記 → `appended` を受信 → `req.destroy()`）＋ SSE 非 admin 403、計 11 tests を追加、`cd server && npm test`（12 files / 130 tests）と `npx tsc --noEmit` が緑）
- 2026-04-23 本番サーバログを外部から安全に閲覧 Phase 2: ログファイル出力とローテーション（`server/` に `pino-roll@^4` を `npm install` で追加、`server/src/lib/logger.ts` を `pino.transport` のマルチターゲット構成に変更し `LOG_FILE_PATH` 環境変数が指定された場合のみ stdout (`pino/file` → fd=1) に加えてファイル出力を有効化・未指定時は従来の同期 stdout pino のまま（テスト・ローカル開発を壊さない）、`pino-roll` の `frequency: 'daily'` + `dateFormat: 'yyyy-MM-dd'` + `limit: { count: 6 }`（active 1 + rotated 6 = 最大 7 ファイル）で日次ローテ、起動時に `fs.mkdirSync(path.dirname(LOG_FILE_PATH), { recursive: true })` で親ディレクトリを先行作成してパーミッションエラーを早期顕在化（pino-roll 側にも `mkdir: true` を渡して二重防御）、`redact` 設定は Phase 1 そのまま維持、`docker-compose.yml` の `environment` に `LOG_FILE_PATH=/app/data/logs/server.log` を追加し既存 volume `./data:/app/data` で自動永続化、`.gitignore` に `data/logs/` を追記（要求メタデータを含むログをコミットしない）、`LOG_FILE_PATH=/tmp/cb-log-smoke/server.log` でスモーク実行し stdout とファイルの両方に同一 JSON Lines が書かれること・`body.otp` が `[REDACTED]` 置換されること・ファイル名が `server.2026-04-23.1.log` となり pino-roll が翌日以降自動で `server.2026-04-24.1.log` を作る仕様であることを確認、`cd server && npm test`（11 files / 119 tests）が緑）
- 2026-04-22 ログインなしで使えるようにする Phase 5: 仕上げ（`mobile/app/(tabs)/_layout.tsx` の☰メニューに `useAiStore.remaining` を「AI 残り X 回」として表示（未ログイン時はログイン項目との区切り線付き、ログイン時は email の下に並べる）、`web/privacy.html` を更新し「1. 収集する情報」にログイン/未ログインの保存先の違いと端末識別子ハッシュ値の項目を追加、新設「4. 端末識別子の取り扱い」で iOS IDFV / Android SSAID を `sha256(raw + DEVICE_ID_SECRET)` でハッシュ化して日次利用回数カウントにのみ使う旨・元の識別子はログ/DB に保存しない旨・広告利用しない旨・ログイン時は送信しない旨を明記、データ保存節も未ログインは端末ローカルに限る旨に修正し最終更新日を 2026 年 4 月 22 日に更新、管理画面に AI 利用状況ビューを追加（`server/src/services/admin-service.ts` に `getAiQuotaStats()` を追加し今日の JST 日付サマリ／直近 14 日の日次集計／キー単位の直近 200 件を返す、`server/src/routes/admin.ts` に `GET /api/admin/ai-quota` を追加、`web/admin/index.html` のサイドバー「システム」グループに「AI 利用状況」リンクを追加、`web/admin/app.js` の `Pages` に `'ai-quota'` を登録し `renderAiQuota()` で今日の呼出数/ユーザー利用/ゲスト利用のサマリカード・日次推移テーブル・キー単位の検索可能テーブル（ゲストはハッシュ先頭 10 桁のみ表示）をレンダリング、`server/src/services/admin-service.ts` の `getSystemInfo` が数える tableCounts に `ai_quota` を追加、ストア提出用説明（`docs/plans/archive/app-store-publish.md`）を「登録不要・ログイン不要ですぐに使い始められます」訴求に更新し未ログイン 3 回/ログイン 20 回の AI 利用回数を明記、Android の simple description も「登録不要で使える買い物リスト」に差し替え、`cd server && npm test`（11 files / 119 tests）と `cd mobile && npm test`（9 files / 56 tests）が緑）
- 2026-04-24 本番サーバログを外部から安全に閲覧 Phase 5: 動作確認（redact 効果、本番実機からの閲覧、SSE の長時間接続）を確認しタスク完了（docs/plans/server-log-viewer.md）
- 2026-04-24 実機アプリの接続先サーバ切り替え Phase 3: 動作確認（`.env` でローカル接続／未設定で本番接続／production ビルドが本番接続）を確認しタスク完了（docs/plans/api-endpoint-switch.md）
- 2026-04-24 テストの導入: 初回 PR で GitHub Actions が両プロジェクト緑になることを確認しタスク完了（docs/plans/testing.md）
- 2026-04-24 AI 利用回数を管理画面から設定可能に（docs/plans/admin-ai-limit-config.md）: `app_settings (key, value, updated_at)` テーブルを `server/src/database.ts` のマイグレーションに追加、`server/src/services/settings-service.ts` 新設で `getAiLimits()`/`setAiLimits()` を提供しモジュールスコープのキャッシュ + 更新時 invalidate、値解決は DB → env (`AI_LIMIT_USER`/`AI_LIMIT_GUEST`) → default (20/3) の優先順位で 0 は許可・負数/非整数/100000 超は `invalid_ai_limit` 例外、`server/src/middleware/rate-limit-ai.ts` の `process.env.AI_LIMIT_*` 直読みを `getAiLimits()` 呼出に置換、`server/src/services/admin-service.ts` の `getAiQuotaStats()` の戻り値に `limits: { user, guest }` を追加、`server/src/routes/admin.ts` に `PUT /api/admin/ai-limits`（body `{ user?, guest? }` を整数バリデーション + sanity 上限 100000、不正は 400 `invalid_ai_limit`、成功で更新後の限度を返す）を追加、`web/admin/app.js` `renderAiQuota()` の先頭に上限編集セクション（user/guest の number input + 保存ボタン + 「0 は AI 機能を実質停止」の注意表示）と各 stat-card の sub に「上限 N」併記を追加、`server/tests/unit/settings-service.test.ts` 新設（env/DB/default フォールバック・キャッシュ・部分更新・0 許可・負数/小数/NaN/文字列/上限超で invalid_ai_limit、計 14 ケース）、`server/tests/integration/ai.test.ts` に `setAiLimits()` 経由で DB が env より優先されることを確認する 3 ケース（guest=1 で 2 回目 429、user=1 で認証ユーザーも 429、limit=0 で即 429）、`server/tests/integration/admin.test.ts` に AI limits ルートを 11 ケース追加（401/403、ai-quota レスポンスへの limits 同梱、フル/部分更新の永続化と再取得反映、0 許可、負数/小数/文字列/上限超/空 body の 400）、`server/tests/helpers/db.ts` の TRUNCATE_TABLES に `ai_quota` と `app_settings` を追加、`cd server && npm test`（15 files / 164 tests）と `npm run build` が緑
- 2026-04-24 料理レシピAI生成を自動で行わないように（docs/plans/recipe-ai-on-demand.md）: `POST /api/ai/suggest` に `mode: 'ingredients' | 'recipes' | 'both'`（省略時 `both` で後方互換）を追加し、`server/src/services/dish-ai.ts` に具材のみ生成する `buildIngredientsOnlyPrompt` を新設、`parseDishInfo` を `recipes` 無しの `{ ingredients }` JSON にも対応、`server/src/routes/ai.ts` で `mode` を受け取り不正値は 400 `invalid_mode`、`server/tests/unit/dish-ai.test.ts` を新設（プロンプト生成 6 ケース＋ parser 7 ケース）、`server/tests/integration/ai.test.ts` に mode ごとの挙動・invalid_mode・mode に依らずクオータ +1 のケースを追加（181 tests 緑）。モバイル: `mobile/src/api/ai.ts` の `suggestAi` に `mode` 引数（デフォルト `both`）追加、`mobile/src/stores/shopping-store.ts` の `suggestIngredients` で `mode='ingredients'` のとき `recipes_json` を温存・`autoSaveRecipes` を呼ばないよう分岐、`mobile/src/components/dishes/IngredientsScreen.tsx` の自動 `useEffect` のみを `mode='ingredients'` に切替えレシピ未生成時に「レシピを生成する」ボタン（`mode='recipes'` で発火）を表示、再取得 / 追加素材で再検索ボタンは現状のまま。`mobile/__tests__` 側も mode を検証するケースを追加（60 tests 緑）。Web: `web/index.html` にレシピ未生成 CTA 行を追加、`web/app.js` の `fetchIngredientsInBackground` を `/api/ai/suggest` の `mode='ingredients'` 経由に変更し結果を `/api/dishes/:id/ai-cache` に書き戻し、レシピ未生成時に「レシピを生成する」ボタンで `mode='recipes'` を投げるハンドラを追加、`web/style.css` に CTA ヒントと refresh ボタン disabled 用スタイルを追加。
- 2026-04-24 通信フローの整理（AI呼び出しを意図せずしないように変更）（docs/specs/ai-call-flow.md）: モバイルの `IngredientsScreen` 初回 `useEffect` の暗黙 `fetchSuggestions(undefined, 'ingredients')` を削除し、AI 呼び出しは必ずユーザーのボタン押下を起点とするよう変更。キャッシュ状態に応じて CTA を出し分け（キャッシュ無し→「具材を AI で取得（残り N 回）」、具材のみ→「レシピを AI で取得（残り N 回）」、両方→「レシピを再検索（残り N 回）」、追加素材あり→「この素材でレシピを再検索（残り N 回）」）、ボタンラベルに `useAiStore.remaining` を併記。空状態（`ingredients.length===0`）に説明テキスト + 取得ボタンの空セクションを追加。`docs/_layouts/default.html` と `dev-admin/src/web/index.html` + `app.js` に Mermaid v11 の CDN ESM を組み込み（kramdown / marked が出力する `<pre><code class="language-mermaid">` を `<pre class="mermaid">` に変換し `mermaid.run({ nodes })` で都度描画、未ロード時は `mermaid-ready` イベントで遅延描画）、本仕様書のフローチャート 2 種が SVG として描画されるようにした。`cd mobile && npm test`（9 files / 60 tests）と `npx tsc --noEmit` が緑。サーバ側の変更は不要（`POST /api/ai/suggest` は `optionalAuth + rateLimitAi` の単一口のまま）。
- 2026-04-25 管理画面から AI 呼び出し回数を初期化可能に（docs/plans/admin-ai-quota-reset.md）: `server/src/services/admin-service.ts` に `resetAiQuota(scope, options?)` を追加し `scope: 'user' | 'guest' | 'all' | 'key'` ごとに `ai_quota` から今日 JST 行のみを DELETE、`server/src/routes/admin.ts` に `POST /api/admin/ai-quota/reset` を追加し scope enum ＋ `scope='key'` 時の `key` 必須 / 正規表現 `^(user:\d+|device:[0-9a-f]{64})$` バリデーション、不正は 400 `invalid_scope`、成功時 `{ scope, deleted }` 返却＋ `logger.info({ scope, deleted, adminUserId, key? }, 'ai_quota_reset')` 監査ログ、`web/admin/app.js` `renderAiQuota()` に「消化数リセット」セクション（user / guest / all の 3 ボタン、`confirm()` + 完了トースト `N 件リセットしました`、失敗トースト）と「キー単位の直近利用」テーブルの各今日行に「今日分リセット」アクション（`visible: row => row.date === today`）を追加、`server/tests/unit/admin-service.test.ts` に 6 ケース（scope ごとの削除範囲・過去日非対象・invalid scope / 未指定 key で例外）、`server/tests/integration/admin.test.ts` に 9 ケース（401/403、scope='all' で todaySummary がゼロ化、user/guest/key の範囲分離、冪等 0 件、invalid_scope 3 パターン）を追加、`server/tests/helpers/db.ts` 触らず既存 TRUNCATE で十分、`cd server && npm test` は 17 files / 206 tests 緑、dev サーバ + 実 admin JWT による E2E 検証でも全 11 API ケース（4 scope + 冪等 + 3 不正 + 401/403 + daily 穴あき無し）通過。
- 2026-04-24 Web アプリ（PWA）削除（docs/plans/web-app-removal.md）: `web/index.html` / `web/app.js` / `web/style.css` / `web/manifest.json` / `web/icon-512.png` / `web/icon_dish.png` を削除し（`icon-192.png` は about 用に `web/img/` へ移動）、`server/src/app.ts` の `GET /` を `res.redirect(301, '/about')` に変更して `__CACHE_VERSION__` 置換を撤去（`X-App-Version` ヘッダは残置）、`web/about.html` の `href="/"` ログインリンクと付随 CSS を削除しアイコン参照を `img/icon-192.png` に変更、PWA 言及の meta description を「モバイルアプリ」に書き換え、`web/admin/app.js` `renderAppName()` の編集対象テーブルを about/admin/mobile に更新し `renderNativeApp()` の現状テーブルを「モバイル公開済み・Web は紹介ページのみ」に更新、`server/tests/integration/app-routing.test.ts` を新設（`/` 301→`/about`、`/about` `/privacy` 200、PWA 旧資産 7 種が 404、`/img/icon-192.png` 200、`/api/health` で X-App-Version 付与）、`CLAUDE.md` / `README.md` の構成図・テーブル・配信先情報を mobile + 紹介ページ前提に書き換え。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 0: 前提確認（docs/plans/admin-auth-cloudflare.md）。本番 `basket.chobi.me` は g3plus（自宅 LAN 10.0.1.10）で稼働中の cloudflared コンテナ（`cloudflare/cloudflared:latest`、172.18.0.2）が `n8n_default` Docker bridge 経由で `cooking-basket:3002`（172.18.0.6）に接続する **Cloudflare Tunnel 経由構成**であることを `g3plus-ops/CLAUDE.md` L40・L56、`g3plus-ops/cooking-basket/docker-compose.yml`、本番への ssh で `docker network inspect n8n_default` を実行して確認。Origin IP は外部に露出しておらず、先行実績として `news-generator`（autopilot.chobi.me）が同構成で Cloudflare Access 認証付きで稼働中。**Phase 5-1 の追加封鎖対応は不要**と確定し、プラン本体（Phase 0 完了マーク・Phase 5-1 「対応不要」化・オープンクエスチョン解消）と TODO.md（Phase 0 チェック済）に反映。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 1: Cloudflare Access の Application / IdP / Policy セットアップ（docs/plans/admin-auth-cloudflare.md）。Cloudflare Zero Trust → Access → Applications で Self-hosted application `basket-admin` を作成（domain `basket.chobi.me`、path `/admin` と `/api/admin`、session 24h）、IdP に既存 Zero Trust テナント `akiraak` の Google を有効化。Team domain `akiraak.cloudflareaccess.com`、issuer `https://akiraak.cloudflareaccess.com`、JWKS `https://akiraak.cloudflareaccess.com/cdn-cgi/access/certs`（RS256 鍵 2 本）、AUD タグを取得し本番 `.env` 投入用にメモ（リポジトリには記録しない）。Policy は Phase 5 まで安全に動かすため `basket-admin-bypass-temp`（Action: Bypass / Include: Everyone）を attach、`basket-admin-allow`（Action: Allow / Include: 管理者 Gmail）は Reusable Policy として作成のみで detach 状態に保ち Phase 5 ステップ 4 で再 attach する運用に統一。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 2: サーバ側 `requireCloudflareAccess` ミドルウェア実装（docs/plans/admin-auth-cloudflare.md）。`server/src/middleware/cloudflare-access.ts` を新設し `jose@^6` を `server/package.json` に追加、`createRemoteJWKSet(url, { cacheMaxAge: 60 * 60 * 1000 })` で JWKS を 1h キャッシュ・`jwtVerify(token, jwks, { audience: CF_ACCESS_AUD, issuer: 'https://<team>.cloudflareaccess.com', algorithms: ['RS256'] })` で `Cf-Access-Jwt-Assertion` ヘッダの JWT を検証（alg=none を含む非 RS256 を拒否）、payload の `email` を `req.adminEmail` にセット（`requireAuth` の `req.userEmail` とフィールド分離して将来の混入を型で検出）、検証失敗・ヘッダ欠落・環境変数未設定はいずれも 401 `Cloudflare Access 認証が必要です` として `logger.warn` で `event=cf_access_*` を出力。dev バイパスは `ADMIN_AUTH_DEV_BYPASS === '1'` ＋ `NODE_ENV in {development, test}` の白リスト判定（`!== 'production'` 否定では未設定/staging で誤発火するため）、有効時は `req.adminEmail = process.env.ADMIN_AUTH_DEV_EMAIL ?? 'dev-admin@local'`。テスト用 `_resetCloudflareAccessJwksCacheForTest()` を export し、`server/tests/helpers/auth.ts` に `startCfAccessStub`/`stopCfAccessStub`/`createCfAccessHeaders(email, overrides)` を追加（`generateKeyPair('RS256')` ＋ ローカル HTTP サーバで `/cdn-cgi/access/certs` を配信し `CF_ACCESS_TEAM_DOMAIN` をスタブ URL に向ける、overrides で aud/iss/exp/foreignKey/algNone/kid を切り替え可能）、`server/tests/integration/admin-cloudflare-auth.test.ts` を新設し正常 / aud 不一致 / iss 不一致 / 期限切れ / ヘッダ欠落 / 署名不正 / alg=none / 環境変数未設定 / dev バイパス白リスト 6 ケースの計 15 ケースを網羅。`server/.env.example` に `CF_ACCESS_TEAM_DOMAIN` / `CF_ACCESS_AUD` / `ADMIN_AUTH_DEV_BYPASS` / `ADMIN_AUTH_DEV_EMAIL` を追記し `ADMIN_EMAILS` を削除（`tests/setup.ts` の `ADMIN_EMAILS` と既存 admin テストは Phase 3 で書き換えるため残置）。`npx vitest run`（207 件）と `npm run build` が緑。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 3: `/api/admin` ルートを Cloudflare Access 認証に付け替え（docs/plans/admin-auth-cloudflare.md）。`server/src/app.ts` で `app.use('/api/admin', cors({ origin: false }), requireCloudflareAccess, adminRouter)` に切替（admin は同一オリジン専用なので CORS を絞る多層防御）、import から `requireAdmin` を外して `requireCloudflareAccess` を追加、`server/src/middleware/auth.ts` から `requireAdmin` 関数定義と export を完全に削除（`Express.Request` の `userId`/`userEmail` は `requireAuth`/`optionalAuth` で引き続き使うため残置）、`server/src/routes/admin.ts` の `ai_quota_reset` 監査ログを `adminUserId: req.userId` から `adminEmail: req.adminEmail` に差し替え、先頭に `GET /me` を追加し `{ success, data: { email: req.adminEmail }, error }` を返す（クライアントのトップバー email 表示用）。`server/tests/setup.ts` から `process.env.ADMIN_EMAILS` を削除、`server/tests/integration/admin.test.ts` を CF Access 経由に書き換え（`createAuthedUser` import を `createCfAccessHeaders`/`startCfAccessStub`/`stopCfAccessStub` に置換、top-level `beforeAll`/`afterAll` で 1 つの JWKS スタブを全 describe で共有、`createAdminHeaders = () => createCfAccessHeaders('admin@test.local')` で機械的置換、旧「403 for a non-admin user」(logs/ai-limits/SSE/ai-quota-reset の 4 箇所) を全削除し旧「401 without Authorization header」を「401 without Cf-Access-Jwt-Assertion header」にリネーム、SSE 結合テストの低レベル `http.request` も `Authorization` から `Cf-Access-Jwt-Assertion` に切替、`describe('GET /api/admin/me')` で正常系と認証失敗の 2 ケース追加）。`npx vitest run`（18 files / 206 件、-4 旧 403 ＋ 2 新規 me で整合）と `npm run build` が緑。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 4: `web/admin` クライアントを Cloudflare Access 経由に改修（docs/plans/admin-auth-cloudflare.md）。`web/admin/app.js` から `getAuthToken()` 関数を削除し `api()` から `Authorization: Bearer` 付与を撤去、`api()` の 401 ハンドラを `location.href = '/cdn-cgi/access/logout?returnTo=' + encodeURIComponent('/admin/')` に変更（`localStorage.removeItem('auth_token'/'auth_email')` も削除）、`fetch` に `credentials: 'same-origin'` を明示して CF Access の `CF_Authorization` Cookie が同一オリジンで自動送出されるよう保険、SSE `connectStream()` でも `Authorization` ヘッダ付与を削除し 401 時の `localStorage` クリア＋`/` 遷移を `/cdn-cgi/access/logout?returnTo=/admin/` 遷移に置換、起動部の `if (!getAuthToken())` ガードを削除し新規 `initAdmin()` で `GET /api/admin/me` を叩いて `res.data.email` をトップバーに反映してから `Router.init()` を呼ぶ（401 は `api()` 内で既に CF Access ログアウトへ遷移するので `initAdmin` 側では何もしない）。`web/admin/index.html` は無変更（「アプリへ戻る」リンクは残置）、静的ファイル `/admin/*` の Origin 直アクセス問題は Phase 0 の確認で Tunnel 経由のため解消済。`grep -n` で `auth_token` / `getAuthToken` / `Authorization` / `Bearer` 参照が完全に消えていることを確認。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 4-2: 管理画面トップバーに Cloudflare Access ログアウトリンクを追加（docs/plans/admin-auth-cloudflare.md）。Phase 4 で `auth_token` 依存を撤去したが SSO セッション（`CF_Authorization` Cookie / 24h）を能動的に切る UI が無かったため、`web/admin/index.html` の `topbar-user` を `<div class="topbar-right">` でラップし隣に `<a id="topbar-logout">ログアウト</a>` を追加、`web/admin/app.js` の `initAdmin()` 冒頭で `topbar-logout` の click を `location.href = '/cdn-cgi/access/logout?returnTo=' + encodeURIComponent('/')` に bind（returnTo を `/` にしてログアウト後はランディングに着地、`/admin/` だと即再ログイン画面に飛ぶため避ける）、`web/admin/style.css` に `.topbar-right`（flex + gap 12px）と `.topbar-logout`（小さめテキスト + 1px ボーダー + hover で背景色変化）を追加。401 時の自動ログアウト（`api()` の 401 ハンドラ）は `returnTo=/admin/` のままにして「セッション切れ→再ログイン誘導」という望ましい挙動を維持。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 5: 本番ロールアウト（docs/plans/admin-auth-cloudflare.md）。Phase 5-2 のステップ 1〜9 を順に実行: ① CF Access ポリシーを Bypass のまま新コードデプロイ準備、② 本番 `.env` から `ADMIN_EMAILS` 削除＋`CF_ACCESS_TEAM_DOMAIN=akiraak.cloudflareaccess.com`／`CF_ACCESS_AUD=<本番投入済>` 追加、③ Phase 2/3/4 のコミットを `git push origin main` → ssh で git pull + `docker compose build/up` → ログで `Server running on http://localhost:3002` 確認、④ CF Access ポリシーを `basket-admin-bypass-temp` から `basket-admin-allow` に切替（途中で IdP 設定漏れが判明し Cloudflare One → Integrations → Identity providers で Google IdP を新規追加、Google Cloud Console で OAuth Client 作成して Authorized redirect URI に `https://akiraak.cloudflareaccess.com/cdn-cgi/access/callback` を登録）、⑤ ブラウザで `https://basket.chobi.me/admin/` → Google SSO → 管理画面表示確認、⑥ curl で 401 検証（外部 `curl /api/admin/me` が 302 + Location が `https://akiraak.cloudflareaccess.com/cdn-cgi/access/login/basket.chobi.me?...` ＋ `www-authenticate: Cloudflare-Access`、ssh で `docker logs cooking-basket --since 24h | grep cf_access` が異常イベント 0 件、g3plus 内から `curl http://localhost:3002/api/admin/me` が 401 + `{"success":false,"data":null,"error":"Cloudflare Access 認証が必要です"}`）、⑦ Origin 直アクセス封鎖は Phase 0 で Tunnel 経由確定済のため対応不要、⑧ `ADMIN_EMAILS` 削除はステップ 2 で同時実施済、⑨ 24 時間監視で `cf_access_misconfigured` / `cf_access_verify_failed` / `cf_access_missing_email` のいずれも 0 件で本番運用が健全であることを確認。
- 2026-04-25 管理画面認証 Cloudflare Access 移行 Phase 6: ドキュメント反映でタスク完了（docs/plans/admin-auth-cloudflare.md）。`README.md` の環境変数表に `CF_ACCESS_TEAM_DOMAIN` / `CF_ACCESS_AUD` / `ADMIN_AUTH_DEV_BYPASS` / `ADMIN_AUTH_DEV_EMAIL` を追記し `JWT_SECRET` の説明を「アプリ向け Magic Link / Google OAuth 用」と明記して admin と分離、表の直下に「管理画面はアプリ認証と独立し本番では Cloudflare Access の Google SSO 前段ゲート＋ Origin の `Cf-Access-Jwt-Assertion` 検証で守る」旨と plan へのリンクを追加。`CLAUDE.md` の「アーキテクチャ上の注意」を改訂し ① アプリ向け認証は `requireAuth` (`req.userId` / `req.userEmail`)、② 管理画面は `requireCloudflareAccess` (`req.adminEmail`) で完全に別系統、③ モバイルアプリは `/api/admin` には到達しないので mobile から admin エンドポイントを叩く実装を入れない、④ `requireAuth` と `requireCloudflareAccess` を同一ルートで重ね掛けしない（フィールド名分離は混入時に型で気づくため）の 4 点を明示し、続けて新セクション「ローカルで管理画面を触るとき」で `server/.env` に `ADMIN_AUTH_DEV_BYPASS=1` / `ADMIN_AUTH_DEV_EMAIL=dev-admin@local` を書く手順を記載（`dev-admin/README.md` は存在しないため CLAUDE.md に集約）。`TODO.md` から該当タスク行と全 Phase チェックを削除し本エントリで DONE.md に移動。
- 2026-04-25 AI 取得フローを「レシピ取得 1 系統」に統一し `createDish` の同名キャッシュ継承を廃止（docs/plans/unify-ai-suggest-flow.md）: サーバ側で `POST /api/ai/suggest` の `mode` パラメータと `SuggestMode` / `VALID_MODES` を撤廃し常に `buildDishInfoPrompt` を使う 1 本道に統一、`server/src/services/dish-ai.ts` から `buildIngredientsOnlyPrompt` と `parseDishInfo` の `{ ingredients: [...] }` 専用分岐を削除、`server/src/services/dish-service.ts` の `createDish` から「同名最新料理を SELECT して `ingredients_json` / `recipes_json` を継承する」ロジックを削除し `INSERT INTO dishes (user_id, name, position) VALUES (?, ?, 0)` のみに、`getDishSuggestions` / `DishSuggestion` と `GET /api/dishes/suggestions` ルートを削除、`shopping-service.ts` の `getSuggestions` / `PurchaseSuggestion` と `GET /api/shopping/suggestions` ルートを削除（`recordPurchase` と `purchase_history` テーブルへの INSERT は admin 画面のために残置）。サーバテストは `tests/integration/ai.test.ts` の `mode parameter` describe・`tests/unit/dish-ai.test.ts` の `buildIngredientsOnlyPrompt` describe・`tests/integration/dishes.test.ts` の `GET /api/dishes/suggestions` describe・`tests/integration/shopping.test.ts` の `GET /api/shopping/suggestions` describe・`tests/unit/shopping-service.test.ts` の `getSuggestions` describe をそれぞれ削除し、`tests/unit/dish-service.test.ts` に「同名再登録時に `ingredients_json` / `recipes_json` が NULL のまま新規作成される」テストを追加（17 files / 192 tests 緑）。モバイル側は `mobile/src/api/ai.ts` の `SuggestAiMode` 型と `suggestAi` の `mode` 引数を削除、`mobile/src/api/dishes.ts` の `getDishSuggestions` と `mobile/src/api/shopping.ts` の `getItemSuggestions`（どちらも UI 未使用）を削除、`mobile/src/stores/shopping-store.ts` の `suggestIngredients` から `mode` 引数と `ingredientsOnly` 分岐を撤廃し常に ingredients + recipes を Zustand に書き戻し + `updateDishAiCache` でサーバ DB 保存（DB キャッシュ機能は維持）、`mobile/src/components/dishes/IngredientsScreen.tsx` から `handleFetchIngredients` / `fetchIngredientsLabel` / empty 状態の専用ボタンを削除、`fetchSuggestions` の `mode` 引数廃止、`refreshLabel` を 2 種類（extras なし→「レシピをAI検索（残り X 回）」、extras あり→「この素材でレシピをAI検索（残り X 回）」）に統合し「再検索」文言を廃止、`ingredients.length === 0` でも extras がある時は「追加素材」セクションを表示してボタンは `handleSearchWithExtras` を呼ぶ、ローディングテキストを「具材を検索中...」から「AI で検索中...」に変更、useEffect での `dish.ingredients_json` / `dish.recipes_json` キャッシュ復元はそのまま維持。モバイルテストは `__tests__/stores/shopping-store.test.ts` の `mode='ingredients'` / `mode='recipes'` 専用ケースと `getDishSuggestions` / `getItemSuggestions` モックを削除、`__tests__/api/ai.test.ts` の mode 引数を含むテストを整理、`__tests__/utils/migration.test.ts` のモックを更新（9 files / 56 tests 緑）。Phase 6 動作確認では curl で `GET /api/dishes/suggestions` と `GET /api/shopping/suggestions` が 404、`POST /api/ai/suggest` に `mode='ingredients'` 付きで送っても 200 で `{ ingredients, recipes }` の通常レスポンスが返ること（旧クライアント互換）を確認、Expo Go 実機で empty 状態の単一ボタン表示・追加素材ありの「この素材でレシピをAI検索」表示・AI 取得後の同文言での再検索・同名再登録時の DB キャッシュ非継承（empty で開く）・admin 画面の `totalPurchases` / `purchase_count` / 購入履歴一覧が引き続き機能することを確認。
- 2026-04-26 ログインコードが違う場合に 401 の英語メッセージではなくサーバの日本語メッセージを表示（docs/plans/login-error-message.md）: `mobile/src/api/client.ts` の axios レスポンスインターセプタに「`error.response?.data?.error` が非空の文字列なら `error.message` をそれで差し替える」処理を追加。これにより `verifyCode` の 401 時に AxiosError がそのまま投げられても AuthModal の `Alert.alert('エラー', e.message)` が `"Request failed with status code 401"` ではなくサーバ側の `"コードが無効または期限切れです"` を表示する。`requestLogin` / `getMe` など他の API も同じ恩恵を受けるよう個別 try/catch ではなくインターセプタ層で対応。既存の 401→`removeToken()` ロジックは保持。サーバ側はメッセージを意図的に「コードが無効または期限切れです」に統一しているのでメッセージ分割はしない（メールアドレス存在の探索や入力ミス↔期限切れ誤判定の吸収のため）。テストは `mobile/__tests__/api/client.test.ts` の `runResponseErrorInterceptor` 型を `data.error` と `message` を含むよう拡張し、① 401 + `data.error` が文字列のとき `error.message` がサーバ文字列に書き換わる、② `data.error` が無いとき `error.message` が元の axios メッセージのまま、③ `data.error` が非文字列（`{ code: 1 }` 等）のとき書き換わらない、の 3 ケースを追加（10 files / 70 tests 緑）。
- 2026-04-26 アプリの入力欄で入力補完機能を有効にする（docs/plans/mobile-input-autocomplete.md）: Phase 1 で `AuthModal.tsx` のメール入力（`textContentType="emailAddress"` / `autoComplete="email"` / `returnKeyType="send"`）と OTP コード入力（`textContentType="oneTimeCode"` / `autoComplete="sms-otp"` / `returnKeyType="done"`）にオートフィル系プロパティを追加（コミット d11694c）。Phase 2 で `AddModal.tsx` / `IngredientsScreen.tsx`（食材名・料理名）に `autoComplete="off"` / `importantForAutofill="no"` / `returnKeyType="done"`、`recipes.tsx` / `shared.tsx`（検索）に `autoComplete="off"` / `importantForAutofill="no"` / `returnKeyType="search"` / `clearButtonMode="while-editing"` を追加し Android Autofill の不要候補を抑制（コミット 422f269）。Phase 3 で iOS / Android 実機確認を実施し、Quick Type / Google Password Manager のメール候補表示、メール OTP のワンタップ入力、食材追加・検索での Autofill 非干渉、検索キーボードの「検索」キー表示・clearButtonMode の × ボタン表示を確認（既存挙動の `autoFocus` / `onSubmitEditing` 等にも変化なし）。本タスクのスコープは OS 標準の autofill / 予測変換のみで、過去の `SuggestionsList.tsx` ベースの履歴サジェスト復活は非スコープ。
- 2026-04-26 dev-admin の plans/specs ドキュメントを 2 ペイン化し左にページ内 TOC を表示（docs/plans/dev-admin-doc-toc-pane.md）: Phase 1 で `dev-admin/src/web/app.js` の `renderMarkdown` を `.content-area > .doc-toolbar` + `.doc-pane-layout > (.doc-toc, .doc-body > .md-content)` 構造に置き換え、`dev-admin/src/web/style.css` に `.doc-pane-layout`（flex + gap 24px + align-items: flex-start）/ `.doc-toc`（flex 0 0 240px / position: sticky / top: 0 / max-height: calc(100vh - 80px) / overflow-y: auto / `:empty { display: none }`）/ `.doc-body`（flex: 1, min-width: 0）/ `.doc-toc-link.active`（accent 色 + 左ボーダー）/ `@media (max-width: 900px) { .doc-toc { display: none } }` を追加。Phase 2 で `buildDocToc(mdContentEl, tocEl)` を実装し `.md-content` 直下の H2〜H4 を走査（H1 は topbar の page-title と重複するため除外）、`slugifyHeading(text, used)` で日本語見出しを `\p{L}\p{N}_-` のみ残しつつ重複時は `-2`/`-3` を suffix する slugger で id 付与、`.doc-toc-item-h{2,3,4}` でレベル別に padding-left を変えた `<ul>` を生成、見出し 2 件未満は早期 return（CSS `:empty` で TOC ペイン非表示）。Phase 3 で `setupTocActiveTracking(headings, linkById)` を実装し IntersectionObserver（root: `.main-content`, rootMargin: `0px 0px -70% 0px`）で「上端 30% の active zone」内の最上位見出しを active 化、ゾーン内に該当が無い場合は `root.top + 16` を基準に直近の上方見出しにフォールバック、初期表示は visible 0 件でも `pickActive` 経由で先頭見出しを active、ルート切替時に `clearTocObserver()` で disconnect する後始末を `renderMarkdown`/`renderTodoView`/`renderDesign`/`showError`/`showEmpty` の先頭にフックして残留 observer を防止。Phase 4 で `dev-admin` (3010) に対し curl で plans/specs API レスポンスの shape 不変・`.doc-pane-layout`/`.doc-toc`/`@media (max-width: 900px)` の CSS 配信・SSE 監視対象が `EDITABLE_FILES`（TODO.md/DONE.md）のみで plans/specs と無関係であることを確認、長尺プラン（11 見出し）/中サイズプラン（8 見出し）/Mermaid 含むスペック（17 見出し + 8 mermaid ブロック）/アーカイブの短プラン（4 見出し）/狭幅 800px /別ドキュメントへの遷移時の observer 再構築を網羅。さらに UX 改善として `.md-content h2` を `#7ab5e6` 背景・`h3` を `#dbeefb` 背景に塗り分けてセクション区切りを視覚化（H1/H4 は背景なしのまま）。Phase 5 で `TODO.md` から該当項目（Phase 1〜4 子タスク含む）を削除し本エントリで `DONE.md` へ移動、`docs/plans/dev-admin-doc-toc-pane.md` を `docs/plans/archive/` へ git mv。
- 2026-04-26 アプリのシンプル化（買物リスト + レシピ履歴のみ、いいね廃止）（docs/plans/archive/app-simplification.md）: 5 Phase で「みんなのレシピ」タブと「いいね」機能をモバイル UI / サーバ API / DB から完全撤去し、「自分のレシピ」を `liked === 1` フィルタなしの履歴的タブに変更。Phase 1（コミット 1e097e2）でサーバから `savedRecipesSharedRouter` / `getSharedRecipes` / `toggleLike` ハンドラ・サービス、`autoSaveRecipes` のいいね復元ロジック、admin 画面の `like_count` 列・集計、`seed-demo.ts` の `recipe_likes` CREATE/INSERT/カウントを削除しテスト群を更新。Phase 2（コミット f6c2c6c）で `mobile/app/(tabs)/_layout.tsx` の `<Tabs.Screen name="shared">` と `TabIcon` の `people: '👥'` を削除、`mobile/app/(tabs)/shared.tsx` 削除、`recipes.tsx` の `mode === 'server' ? savedRecipes.filter((r) => r.liked) : savedRecipes` を `savedRecipes` に置換し `toggleLike` 関連ハンドラ撤去、`RecipeListItem` / `RecipeCard` / `IngredientsScreen` からハート UI と `.heart` スタイルを除去。Phase 3（コミット eb42e44）で `models.ts` の `RecipeState.liked` / `like_count` と `SavedRecipe.liked` / `like_count` を削除、`api/saved-recipes.ts` の `getSharedRecipes` / `toggleLike` を削除、`recipe-store` の `sharedRecipes` / `loadSharedRecipes` / `toggleLike` / `partialize` を整理、`shopping-store` の `recipeStates` 型から `liked` / `like_count` を削除し全モバイルテストをグリーン化。Phase 4（コミット f00b9e1）で `server/src/database.ts` 末尾に `DROP TABLE IF EXISTS recipe_likes` の追加マイグレーションを入れ、起動時に本番 DB から該当テーブルが消えるようにした（`saved_recipes.liked` カラムは SQLite 再構築コストを避けてコード参照を断つだけに留める）。Phase 5 で `cd server && npm test`（20 files / 216 tests 緑）と `cd mobile && npm test`（11 suites / 74 tests 緑）を確認、本プラン・`my-recipes-display-criteria.md`・`my-recipes-migrate-likes.md` を `docs/plans/archive/` へ移動、TODO.md からシンプル化親子タスクと「自分のレシピに表示されるレシピの判定基準の調査」「(保留) migrate API でローカル savedRecipes をいいね済みとして取り込む」「ハートをフラットなイラストに」を削除。スコープ外として `autoSaveRecipes` の `source_dish_id` 単位上書き仕様、タブ名「自分のレシピ」、`saved_recipes.liked` カラム自体のドロップ、個別削除 UI 追加は維持。リリース順序はモバイル先行（Phase 2-3 → ストア配布 → 自動更新待ち）→ サーバ Phase 1 デプロイ → DB Phase 4 マイグレーション（直前に本番 DB バックアップ）の順で行う想定。
- 2026-04-26 空状態メッセージの改善（レシピノート）（docs/plans/archive/empty-state-messages.md）: レシピノートタブの 0 件時の文言を「レシピを生成すると自動で保存されます」から「買い物リストで料理レシピを検索すると表示されます」に変更（`mobile/app/(tabs)/recipes.tsx:70`）。当初プランでは買い物リスト側（`mobile/app/(tabs)/index.tsx:320` の `リストは空です。料理や食材を追加しましょう`）も対象だったが、Phase 0 の実機確認（`isEmpty = true` を一時化）で現状文言・余白に違和感なしと判断し、買い物リスト側はスコープから除外して現状維持とした。実機確認時の空状態描画は Phase 0 / Phase 2 とも `mobile/app/(tabs)/recipes.tsx` の `FlatList data={filtered}` を一時的に `data={[]}` に差し替え＋ `index.tsx` の `isEmpty` を `true` にする形で行い、確認後 revert（コードに永続変更なし）。スタイル `emptyText` / レイアウト / `recipe-store` / `shopping-store` / 検索結果 0 件分岐 (`search ? '検索結果なし' : ...`) は不変。`cd mobile && npm test` 11 suites / 74 tests 緑（旧文言の残存 grep ゼロ）。
- 2026-04-27 アプリアイコンのボウル→バスケット差し替え（docs/plans/archive/app-icon-basket.md）: 新素材 `cooking-basket-icon.png`（1254×1254、白背景、オレンジのバスケット＋牛乳パック・りんご・ワイン・バゲット＋チェックマーク）から ImageMagick で 6 アセットを再生成。`mobile/assets/icon.png`（1024×1024、白背景）と `mobile/assets/splash-icon.png`（同）はソースをそのまま 1024 へリサイズ、`mobile/assets/adaptive-icon.png` は Android 66% safe zone に収めるためソースを 865×865 に縮小して 1024×1024 白キャンバス中央へ extent（外周 17% パディング）、`mobile/assets/favicon.png`（48×48）は `-fuzz 2% -transparent white` で白を透過化してから 48 へ縮小、`web/img/icon-192.png`（192×192、白背景）は単純リサイズ、`web/img/ogp.png`（1200×630、白背景）は中央に 325×325 のアイコンを compose（既存 OGP の content サイズと同等）。サイズ・パス・ファイル名は据え置きのため `mobile/app.json`（`./assets/icon.png` 等の参照）/ `web/about.html`（`<img src="img/icon-192.png">` と OGP meta）/ `server/tests/integration/app-routing.test.ts`（`/img/icon-192.png` 200 期待）に変更不要。`cd server && npm test`（20 files / 216 tests 緑）と `cd mobile && npm test`（11 suites / 74 tests 緑）が通ることを確認。スコープ外として `mobile/assets/icon_dish.png`（レシピタブ FAB 内のアイコン）、Bundle ID / Expo project ID / app name / splash `backgroundColor: "#1c1c1c"` は不変。**ストア掲載アイコンの差し替えは次回 `eas build -p ios/android --profile production` でビルド焼き直し → `eas submit` 後に反映される**（本タスクではビルドしない）。実機 / Simulator / ブラウザでの最終目視確認はユーザー側で実施予定。
- 2026-04-26 ログインのパスコード入力画面での「別のメールアドレスで試す」を削除し「ログインコードの再送」を入れる（docs/plans/auth-modal-resend-code.md / コミット e123121）: `mobile/src/components/auth/AuthModal.tsx` の `step === 'code'` ブロックから `handleBackToEmail` のリンクを削除し、同位置に「ログインコードの再送」リンクを追加。`handleResendCode` は既存 `sendMagicCode(email)`（`/api/auth/login`）をそのまま再利用し、サーバ側 `createMagicLinkToken` が呼ばれるたび既存未使用トークンを `used=1` にして新コードを発行する挙動に乗る（サーバ側変更なし）。連打防止に 30 秒のフロントクールダウンを追加: `const [resendCooldown, setResendCooldown] = useState(0)` を 30 にセットし `setInterval` で 1 秒ずつデクリメント、0 で停止、`useEffect` cleanup で interval を解放、モーダル再表示の初期化 useEffect でも 0 にリセット。クールダウン中はラベルを「再送（残り N 秒）」にして `disabled`、`loading` 中も `disabled`。成功時は `Alert.alert('送信しました', '新しいコードをメールで送信しました')`、失敗時は既存 `handleSendCode` と同じ Alert パターン。「キャンセル」リンクはメール変更したい場合の動線として残置。テストは AuthModal の RN レンダリングテストが未導入のため追加なし。手動確認で再送 → 30 秒クールダウン表示 → 経過後再押下可能 → 旧コードが「コードが無効または期限切れです」になることを確認（`cd mobile && npm test` 9 files / 56 tests 緑）。スコープ外: サーバ側 `/api/auth/login` のレートリミット、メール入力ステップへ戻る別 UI、Google ログイン動線変更。
